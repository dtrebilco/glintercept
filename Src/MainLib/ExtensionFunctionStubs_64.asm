; This file is generated by generate_64b_asm_stubs.py script.
; Do not modify. For more comments refer to generator script.
.code 
EXTERN wrapperIndex : QWORD
EXTERN extFunctions : QWORD

WrapperLogFunctionPre PROTO
WrapperLogFunctionPost PROTO


WrapperCall0 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+0*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+0*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+0*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall0 ENDP

WrapperCall1 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1 ENDP

WrapperCall2 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+2*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+2*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+2*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall2 ENDP

WrapperCall3 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+3*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+3*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+3*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall3 ENDP

WrapperCall4 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+4*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+4*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+4*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall4 ENDP

WrapperCall5 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+5*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+5*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+5*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall5 ENDP

WrapperCall6 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+6*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+6*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+6*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall6 ENDP

WrapperCall7 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+7*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+7*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+7*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall7 ENDP

WrapperCall8 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+8*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+8*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+8*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall8 ENDP

WrapperCall9 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+9*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+9*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+9*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall9 ENDP

WrapperCall10 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+10*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+10*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+10*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall10 ENDP

WrapperCall11 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+11*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+11*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+11*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall11 ENDP

WrapperCall12 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+12*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+12*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+12*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall12 ENDP

WrapperCall13 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+13*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+13*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+13*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall13 ENDP

WrapperCall14 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+14*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+14*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+14*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall14 ENDP

WrapperCall15 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+15*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+15*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+15*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall15 ENDP

WrapperCall16 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+16*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+16*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+16*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall16 ENDP

WrapperCall17 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+17*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+17*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+17*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall17 ENDP

WrapperCall18 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+18*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+18*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+18*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall18 ENDP

WrapperCall19 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+19*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+19*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+19*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall19 ENDP

WrapperCall20 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+20*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+20*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+20*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall20 ENDP

WrapperCall21 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+21*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+21*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+21*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall21 ENDP

WrapperCall22 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+22*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+22*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+22*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall22 ENDP

WrapperCall23 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+23*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+23*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+23*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall23 ENDP

WrapperCall24 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+24*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+24*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+24*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall24 ENDP

WrapperCall25 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+25*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+25*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+25*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall25 ENDP

WrapperCall26 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+26*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+26*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+26*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall26 ENDP

WrapperCall27 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+27*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+27*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+27*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall27 ENDP

WrapperCall28 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+28*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+28*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+28*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall28 ENDP

WrapperCall29 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+29*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+29*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+29*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall29 ENDP

WrapperCall30 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+30*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+30*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+30*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall30 ENDP

WrapperCall31 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+31*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+31*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+31*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall31 ENDP

WrapperCall32 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+32*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+32*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+32*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall32 ENDP

WrapperCall33 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+33*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+33*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+33*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall33 ENDP

WrapperCall34 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+34*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+34*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+34*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall34 ENDP

WrapperCall35 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+35*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+35*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+35*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall35 ENDP

WrapperCall36 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+36*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+36*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+36*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall36 ENDP

WrapperCall37 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+37*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+37*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+37*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall37 ENDP

WrapperCall38 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+38*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+38*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+38*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall38 ENDP

WrapperCall39 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+39*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+39*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+39*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall39 ENDP

WrapperCall40 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+40*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+40*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+40*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall40 ENDP

WrapperCall41 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+41*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+41*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+41*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall41 ENDP

WrapperCall42 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+42*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+42*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+42*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall42 ENDP

WrapperCall43 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+43*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+43*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+43*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall43 ENDP

WrapperCall44 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+44*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+44*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+44*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall44 ENDP

WrapperCall45 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+45*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+45*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+45*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall45 ENDP

WrapperCall46 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+46*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+46*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+46*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall46 ENDP

WrapperCall47 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+47*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+47*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+47*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall47 ENDP

WrapperCall48 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+48*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+48*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+48*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall48 ENDP

WrapperCall49 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+49*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+49*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+49*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall49 ENDP

WrapperCall50 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+50*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+50*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+50*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall50 ENDP

WrapperCall51 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+51*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+51*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+51*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall51 ENDP

WrapperCall52 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+52*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+52*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+52*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall52 ENDP

WrapperCall53 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+53*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+53*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+53*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall53 ENDP

WrapperCall54 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+54*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+54*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+54*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall54 ENDP

WrapperCall55 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+55*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+55*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+55*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall55 ENDP

WrapperCall56 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+56*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+56*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+56*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall56 ENDP

WrapperCall57 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+57*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+57*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+57*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall57 ENDP

WrapperCall58 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+58*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+58*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+58*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall58 ENDP

WrapperCall59 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+59*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+59*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+59*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall59 ENDP

WrapperCall60 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+60*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+60*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+60*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall60 ENDP

WrapperCall61 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+61*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+61*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+61*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall61 ENDP

WrapperCall62 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+62*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+62*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+62*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall62 ENDP

WrapperCall63 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+63*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+63*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+63*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall63 ENDP

WrapperCall64 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+64*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+64*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+64*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall64 ENDP

WrapperCall65 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+65*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+65*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+65*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall65 ENDP

WrapperCall66 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+66*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+66*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+66*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall66 ENDP

WrapperCall67 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+67*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+67*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+67*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall67 ENDP

WrapperCall68 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+68*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+68*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+68*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall68 ENDP

WrapperCall69 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+69*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+69*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+69*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall69 ENDP

WrapperCall70 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+70*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+70*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+70*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall70 ENDP

WrapperCall71 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+71*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+71*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+71*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall71 ENDP

WrapperCall72 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+72*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+72*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+72*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall72 ENDP

WrapperCall73 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+73*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+73*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+73*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall73 ENDP

WrapperCall74 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+74*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+74*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+74*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall74 ENDP

WrapperCall75 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+75*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+75*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+75*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall75 ENDP

WrapperCall76 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+76*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+76*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+76*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall76 ENDP

WrapperCall77 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+77*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+77*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+77*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall77 ENDP

WrapperCall78 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+78*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+78*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+78*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall78 ENDP

WrapperCall79 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+79*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+79*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+79*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall79 ENDP

WrapperCall80 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+80*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+80*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+80*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall80 ENDP

WrapperCall81 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+81*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+81*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+81*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall81 ENDP

WrapperCall82 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+82*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+82*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+82*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall82 ENDP

WrapperCall83 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+83*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+83*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+83*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall83 ENDP

WrapperCall84 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+84*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+84*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+84*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall84 ENDP

WrapperCall85 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+85*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+85*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+85*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall85 ENDP

WrapperCall86 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+86*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+86*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+86*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall86 ENDP

WrapperCall87 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+87*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+87*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+87*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall87 ENDP

WrapperCall88 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+88*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+88*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+88*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall88 ENDP

WrapperCall89 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+89*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+89*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+89*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall89 ENDP

WrapperCall90 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+90*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+90*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+90*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall90 ENDP

WrapperCall91 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+91*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+91*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+91*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall91 ENDP

WrapperCall92 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+92*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+92*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+92*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall92 ENDP

WrapperCall93 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+93*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+93*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+93*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall93 ENDP

WrapperCall94 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+94*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+94*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+94*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall94 ENDP

WrapperCall95 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+95*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+95*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+95*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall95 ENDP

WrapperCall96 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+96*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+96*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+96*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall96 ENDP

WrapperCall97 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+97*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+97*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+97*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall97 ENDP

WrapperCall98 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+98*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+98*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+98*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall98 ENDP

WrapperCall99 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+99*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+99*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+99*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall99 ENDP

WrapperCall100 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+100*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+100*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+100*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall100 ENDP

WrapperCall101 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+101*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+101*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+101*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall101 ENDP

WrapperCall102 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+102*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+102*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+102*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall102 ENDP

WrapperCall103 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+103*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+103*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+103*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall103 ENDP

WrapperCall104 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+104*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+104*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+104*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall104 ENDP

WrapperCall105 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+105*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+105*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+105*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall105 ENDP

WrapperCall106 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+106*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+106*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+106*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall106 ENDP

WrapperCall107 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+107*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+107*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+107*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall107 ENDP

WrapperCall108 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+108*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+108*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+108*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall108 ENDP

WrapperCall109 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+109*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+109*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+109*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall109 ENDP

WrapperCall110 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+110*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+110*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+110*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall110 ENDP

WrapperCall111 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+111*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+111*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+111*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall111 ENDP

WrapperCall112 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+112*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+112*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+112*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall112 ENDP

WrapperCall113 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+113*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+113*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+113*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall113 ENDP

WrapperCall114 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+114*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+114*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+114*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall114 ENDP

WrapperCall115 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+115*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+115*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+115*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall115 ENDP

WrapperCall116 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+116*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+116*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+116*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall116 ENDP

WrapperCall117 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+117*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+117*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+117*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall117 ENDP

WrapperCall118 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+118*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+118*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+118*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall118 ENDP

WrapperCall119 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+119*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+119*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+119*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall119 ENDP

WrapperCall120 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+120*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+120*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+120*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall120 ENDP

WrapperCall121 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+121*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+121*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+121*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall121 ENDP

WrapperCall122 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+122*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+122*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+122*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall122 ENDP

WrapperCall123 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+123*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+123*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+123*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall123 ENDP

WrapperCall124 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+124*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+124*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+124*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall124 ENDP

WrapperCall125 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+125*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+125*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+125*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall125 ENDP

WrapperCall126 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+126*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+126*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+126*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall126 ENDP

WrapperCall127 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+127*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+127*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+127*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall127 ENDP

WrapperCall128 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+128*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+128*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+128*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall128 ENDP

WrapperCall129 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+129*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+129*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+129*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall129 ENDP

WrapperCall130 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+130*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+130*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+130*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall130 ENDP

WrapperCall131 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+131*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+131*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+131*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall131 ENDP

WrapperCall132 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+132*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+132*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+132*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall132 ENDP

WrapperCall133 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+133*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+133*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+133*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall133 ENDP

WrapperCall134 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+134*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+134*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+134*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall134 ENDP

WrapperCall135 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+135*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+135*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+135*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall135 ENDP

WrapperCall136 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+136*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+136*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+136*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall136 ENDP

WrapperCall137 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+137*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+137*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+137*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall137 ENDP

WrapperCall138 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+138*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+138*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+138*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall138 ENDP

WrapperCall139 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+139*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+139*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+139*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall139 ENDP

WrapperCall140 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+140*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+140*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+140*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall140 ENDP

WrapperCall141 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+141*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+141*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+141*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall141 ENDP

WrapperCall142 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+142*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+142*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+142*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall142 ENDP

WrapperCall143 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+143*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+143*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+143*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall143 ENDP

WrapperCall144 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+144*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+144*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+144*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall144 ENDP

WrapperCall145 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+145*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+145*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+145*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall145 ENDP

WrapperCall146 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+146*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+146*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+146*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall146 ENDP

WrapperCall147 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+147*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+147*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+147*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall147 ENDP

WrapperCall148 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+148*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+148*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+148*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall148 ENDP

WrapperCall149 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+149*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+149*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+149*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall149 ENDP

WrapperCall150 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+150*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+150*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+150*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall150 ENDP

WrapperCall151 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+151*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+151*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+151*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall151 ENDP

WrapperCall152 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+152*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+152*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+152*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall152 ENDP

WrapperCall153 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+153*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+153*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+153*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall153 ENDP

WrapperCall154 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+154*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+154*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+154*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall154 ENDP

WrapperCall155 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+155*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+155*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+155*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall155 ENDP

WrapperCall156 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+156*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+156*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+156*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall156 ENDP

WrapperCall157 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+157*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+157*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+157*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall157 ENDP

WrapperCall158 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+158*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+158*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+158*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall158 ENDP

WrapperCall159 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+159*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+159*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+159*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall159 ENDP

WrapperCall160 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+160*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+160*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+160*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall160 ENDP

WrapperCall161 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+161*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+161*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+161*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall161 ENDP

WrapperCall162 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+162*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+162*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+162*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall162 ENDP

WrapperCall163 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+163*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+163*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+163*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall163 ENDP

WrapperCall164 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+164*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+164*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+164*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall164 ENDP

WrapperCall165 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+165*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+165*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+165*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall165 ENDP

WrapperCall166 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+166*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+166*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+166*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall166 ENDP

WrapperCall167 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+167*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+167*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+167*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall167 ENDP

WrapperCall168 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+168*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+168*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+168*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall168 ENDP

WrapperCall169 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+169*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+169*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+169*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall169 ENDP

WrapperCall170 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+170*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+170*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+170*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall170 ENDP

WrapperCall171 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+171*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+171*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+171*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall171 ENDP

WrapperCall172 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+172*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+172*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+172*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall172 ENDP

WrapperCall173 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+173*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+173*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+173*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall173 ENDP

WrapperCall174 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+174*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+174*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+174*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall174 ENDP

WrapperCall175 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+175*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+175*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+175*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall175 ENDP

WrapperCall176 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+176*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+176*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+176*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall176 ENDP

WrapperCall177 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+177*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+177*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+177*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall177 ENDP

WrapperCall178 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+178*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+178*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+178*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall178 ENDP

WrapperCall179 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+179*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+179*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+179*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall179 ENDP

WrapperCall180 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+180*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+180*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+180*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall180 ENDP

WrapperCall181 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+181*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+181*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+181*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall181 ENDP

WrapperCall182 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+182*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+182*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+182*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall182 ENDP

WrapperCall183 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+183*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+183*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+183*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall183 ENDP

WrapperCall184 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+184*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+184*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+184*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall184 ENDP

WrapperCall185 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+185*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+185*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+185*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall185 ENDP

WrapperCall186 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+186*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+186*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+186*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall186 ENDP

WrapperCall187 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+187*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+187*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+187*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall187 ENDP

WrapperCall188 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+188*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+188*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+188*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall188 ENDP

WrapperCall189 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+189*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+189*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+189*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall189 ENDP

WrapperCall190 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+190*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+190*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+190*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall190 ENDP

WrapperCall191 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+191*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+191*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+191*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall191 ENDP

WrapperCall192 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+192*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+192*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+192*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall192 ENDP

WrapperCall193 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+193*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+193*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+193*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall193 ENDP

WrapperCall194 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+194*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+194*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+194*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall194 ENDP

WrapperCall195 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+195*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+195*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+195*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall195 ENDP

WrapperCall196 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+196*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+196*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+196*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall196 ENDP

WrapperCall197 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+197*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+197*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+197*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall197 ENDP

WrapperCall198 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+198*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+198*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+198*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall198 ENDP

WrapperCall199 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+199*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+199*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+199*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall199 ENDP

WrapperCall200 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+200*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+200*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+200*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall200 ENDP

WrapperCall201 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+201*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+201*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+201*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall201 ENDP

WrapperCall202 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+202*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+202*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+202*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall202 ENDP

WrapperCall203 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+203*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+203*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+203*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall203 ENDP

WrapperCall204 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+204*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+204*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+204*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall204 ENDP

WrapperCall205 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+205*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+205*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+205*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall205 ENDP

WrapperCall206 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+206*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+206*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+206*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall206 ENDP

WrapperCall207 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+207*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+207*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+207*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall207 ENDP

WrapperCall208 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+208*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+208*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+208*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall208 ENDP

WrapperCall209 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+209*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+209*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+209*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall209 ENDP

WrapperCall210 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+210*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+210*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+210*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall210 ENDP

WrapperCall211 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+211*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+211*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+211*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall211 ENDP

WrapperCall212 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+212*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+212*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+212*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall212 ENDP

WrapperCall213 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+213*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+213*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+213*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall213 ENDP

WrapperCall214 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+214*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+214*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+214*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall214 ENDP

WrapperCall215 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+215*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+215*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+215*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall215 ENDP

WrapperCall216 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+216*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+216*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+216*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall216 ENDP

WrapperCall217 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+217*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+217*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+217*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall217 ENDP

WrapperCall218 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+218*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+218*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+218*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall218 ENDP

WrapperCall219 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+219*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+219*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+219*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall219 ENDP

WrapperCall220 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+220*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+220*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+220*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall220 ENDP

WrapperCall221 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+221*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+221*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+221*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall221 ENDP

WrapperCall222 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+222*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+222*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+222*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall222 ENDP

WrapperCall223 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+223*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+223*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+223*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall223 ENDP

WrapperCall224 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+224*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+224*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+224*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall224 ENDP

WrapperCall225 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+225*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+225*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+225*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall225 ENDP

WrapperCall226 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+226*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+226*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+226*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall226 ENDP

WrapperCall227 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+227*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+227*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+227*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall227 ENDP

WrapperCall228 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+228*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+228*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+228*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall228 ENDP

WrapperCall229 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+229*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+229*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+229*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall229 ENDP

WrapperCall230 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+230*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+230*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+230*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall230 ENDP

WrapperCall231 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+231*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+231*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+231*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall231 ENDP

WrapperCall232 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+232*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+232*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+232*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall232 ENDP

WrapperCall233 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+233*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+233*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+233*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall233 ENDP

WrapperCall234 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+234*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+234*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+234*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall234 ENDP

WrapperCall235 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+235*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+235*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+235*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall235 ENDP

WrapperCall236 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+236*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+236*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+236*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall236 ENDP

WrapperCall237 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+237*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+237*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+237*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall237 ENDP

WrapperCall238 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+238*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+238*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+238*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall238 ENDP

WrapperCall239 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+239*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+239*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+239*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall239 ENDP

WrapperCall240 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+240*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+240*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+240*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall240 ENDP

WrapperCall241 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+241*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+241*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+241*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall241 ENDP

WrapperCall242 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+242*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+242*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+242*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall242 ENDP

WrapperCall243 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+243*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+243*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+243*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall243 ENDP

WrapperCall244 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+244*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+244*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+244*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall244 ENDP

WrapperCall245 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+245*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+245*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+245*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall245 ENDP

WrapperCall246 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+246*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+246*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+246*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall246 ENDP

WrapperCall247 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+247*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+247*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+247*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall247 ENDP

WrapperCall248 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+248*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+248*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+248*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall248 ENDP

WrapperCall249 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+249*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+249*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+249*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall249 ENDP

WrapperCall250 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+250*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+250*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+250*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall250 ENDP

WrapperCall251 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+251*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+251*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+251*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall251 ENDP

WrapperCall252 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+252*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+252*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+252*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall252 ENDP

WrapperCall253 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+253*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+253*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+253*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall253 ENDP

WrapperCall254 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+254*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+254*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+254*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall254 ENDP

WrapperCall255 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+255*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+255*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+255*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall255 ENDP

WrapperCall256 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+256*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+256*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+256*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall256 ENDP

WrapperCall257 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+257*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+257*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+257*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall257 ENDP

WrapperCall258 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+258*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+258*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+258*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall258 ENDP

WrapperCall259 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+259*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+259*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+259*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall259 ENDP

WrapperCall260 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+260*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+260*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+260*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall260 ENDP

WrapperCall261 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+261*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+261*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+261*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall261 ENDP

WrapperCall262 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+262*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+262*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+262*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall262 ENDP

WrapperCall263 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+263*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+263*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+263*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall263 ENDP

WrapperCall264 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+264*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+264*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+264*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall264 ENDP

WrapperCall265 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+265*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+265*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+265*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall265 ENDP

WrapperCall266 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+266*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+266*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+266*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall266 ENDP

WrapperCall267 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+267*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+267*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+267*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall267 ENDP

WrapperCall268 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+268*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+268*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+268*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall268 ENDP

WrapperCall269 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+269*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+269*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+269*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall269 ENDP

WrapperCall270 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+270*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+270*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+270*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall270 ENDP

WrapperCall271 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+271*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+271*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+271*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall271 ENDP

WrapperCall272 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+272*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+272*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+272*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall272 ENDP

WrapperCall273 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+273*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+273*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+273*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall273 ENDP

WrapperCall274 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+274*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+274*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+274*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall274 ENDP

WrapperCall275 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+275*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+275*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+275*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall275 ENDP

WrapperCall276 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+276*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+276*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+276*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall276 ENDP

WrapperCall277 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+277*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+277*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+277*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall277 ENDP

WrapperCall278 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+278*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+278*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+278*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall278 ENDP

WrapperCall279 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+279*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+279*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+279*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall279 ENDP

WrapperCall280 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+280*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+280*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+280*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall280 ENDP

WrapperCall281 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+281*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+281*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+281*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall281 ENDP

WrapperCall282 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+282*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+282*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+282*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall282 ENDP

WrapperCall283 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+283*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+283*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+283*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall283 ENDP

WrapperCall284 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+284*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+284*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+284*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall284 ENDP

WrapperCall285 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+285*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+285*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+285*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall285 ENDP

WrapperCall286 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+286*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+286*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+286*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall286 ENDP

WrapperCall287 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+287*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+287*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+287*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall287 ENDP

WrapperCall288 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+288*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+288*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+288*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall288 ENDP

WrapperCall289 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+289*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+289*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+289*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall289 ENDP

WrapperCall290 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+290*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+290*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+290*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall290 ENDP

WrapperCall291 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+291*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+291*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+291*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall291 ENDP

WrapperCall292 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+292*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+292*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+292*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall292 ENDP

WrapperCall293 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+293*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+293*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+293*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall293 ENDP

WrapperCall294 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+294*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+294*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+294*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall294 ENDP

WrapperCall295 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+295*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+295*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+295*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall295 ENDP

WrapperCall296 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+296*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+296*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+296*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall296 ENDP

WrapperCall297 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+297*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+297*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+297*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall297 ENDP

WrapperCall298 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+298*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+298*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+298*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall298 ENDP

WrapperCall299 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+299*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+299*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+299*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall299 ENDP

WrapperCall300 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+300*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+300*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+300*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall300 ENDP

WrapperCall301 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+301*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+301*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+301*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall301 ENDP

WrapperCall302 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+302*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+302*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+302*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall302 ENDP

WrapperCall303 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+303*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+303*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+303*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall303 ENDP

WrapperCall304 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+304*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+304*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+304*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall304 ENDP

WrapperCall305 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+305*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+305*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+305*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall305 ENDP

WrapperCall306 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+306*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+306*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+306*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall306 ENDP

WrapperCall307 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+307*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+307*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+307*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall307 ENDP

WrapperCall308 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+308*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+308*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+308*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall308 ENDP

WrapperCall309 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+309*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+309*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+309*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall309 ENDP

WrapperCall310 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+310*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+310*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+310*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall310 ENDP

WrapperCall311 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+311*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+311*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+311*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall311 ENDP

WrapperCall312 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+312*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+312*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+312*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall312 ENDP

WrapperCall313 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+313*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+313*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+313*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall313 ENDP

WrapperCall314 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+314*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+314*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+314*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall314 ENDP

WrapperCall315 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+315*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+315*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+315*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall315 ENDP

WrapperCall316 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+316*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+316*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+316*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall316 ENDP

WrapperCall317 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+317*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+317*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+317*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall317 ENDP

WrapperCall318 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+318*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+318*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+318*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall318 ENDP

WrapperCall319 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+319*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+319*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+319*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall319 ENDP

WrapperCall320 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+320*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+320*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+320*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall320 ENDP

WrapperCall321 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+321*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+321*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+321*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall321 ENDP

WrapperCall322 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+322*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+322*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+322*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall322 ENDP

WrapperCall323 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+323*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+323*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+323*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall323 ENDP

WrapperCall324 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+324*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+324*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+324*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall324 ENDP

WrapperCall325 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+325*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+325*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+325*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall325 ENDP

WrapperCall326 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+326*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+326*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+326*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall326 ENDP

WrapperCall327 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+327*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+327*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+327*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall327 ENDP

WrapperCall328 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+328*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+328*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+328*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall328 ENDP

WrapperCall329 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+329*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+329*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+329*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall329 ENDP

WrapperCall330 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+330*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+330*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+330*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall330 ENDP

WrapperCall331 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+331*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+331*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+331*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall331 ENDP

WrapperCall332 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+332*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+332*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+332*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall332 ENDP

WrapperCall333 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+333*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+333*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+333*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall333 ENDP

WrapperCall334 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+334*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+334*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+334*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall334 ENDP

WrapperCall335 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+335*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+335*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+335*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall335 ENDP

WrapperCall336 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+336*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+336*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+336*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall336 ENDP

WrapperCall337 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+337*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+337*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+337*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall337 ENDP

WrapperCall338 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+338*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+338*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+338*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall338 ENDP

WrapperCall339 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+339*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+339*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+339*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall339 ENDP

WrapperCall340 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+340*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+340*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+340*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall340 ENDP

WrapperCall341 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+341*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+341*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+341*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall341 ENDP

WrapperCall342 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+342*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+342*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+342*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall342 ENDP

WrapperCall343 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+343*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+343*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+343*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall343 ENDP

WrapperCall344 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+344*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+344*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+344*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall344 ENDP

WrapperCall345 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+345*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+345*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+345*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall345 ENDP

WrapperCall346 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+346*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+346*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+346*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall346 ENDP

WrapperCall347 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+347*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+347*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+347*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall347 ENDP

WrapperCall348 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+348*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+348*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+348*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall348 ENDP

WrapperCall349 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+349*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+349*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+349*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall349 ENDP

WrapperCall350 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+350*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+350*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+350*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall350 ENDP

WrapperCall351 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+351*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+351*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+351*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall351 ENDP

WrapperCall352 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+352*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+352*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+352*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall352 ENDP

WrapperCall353 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+353*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+353*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+353*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall353 ENDP

WrapperCall354 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+354*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+354*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+354*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall354 ENDP

WrapperCall355 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+355*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+355*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+355*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall355 ENDP

WrapperCall356 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+356*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+356*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+356*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall356 ENDP

WrapperCall357 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+357*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+357*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+357*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall357 ENDP

WrapperCall358 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+358*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+358*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+358*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall358 ENDP

WrapperCall359 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+359*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+359*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+359*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall359 ENDP

WrapperCall360 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+360*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+360*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+360*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall360 ENDP

WrapperCall361 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+361*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+361*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+361*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall361 ENDP

WrapperCall362 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+362*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+362*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+362*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall362 ENDP

WrapperCall363 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+363*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+363*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+363*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall363 ENDP

WrapperCall364 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+364*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+364*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+364*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall364 ENDP

WrapperCall365 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+365*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+365*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+365*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall365 ENDP

WrapperCall366 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+366*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+366*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+366*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall366 ENDP

WrapperCall367 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+367*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+367*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+367*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall367 ENDP

WrapperCall368 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+368*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+368*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+368*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall368 ENDP

WrapperCall369 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+369*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+369*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+369*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall369 ENDP

WrapperCall370 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+370*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+370*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+370*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall370 ENDP

WrapperCall371 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+371*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+371*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+371*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall371 ENDP

WrapperCall372 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+372*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+372*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+372*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall372 ENDP

WrapperCall373 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+373*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+373*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+373*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall373 ENDP

WrapperCall374 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+374*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+374*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+374*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall374 ENDP

WrapperCall375 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+375*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+375*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+375*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall375 ENDP

WrapperCall376 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+376*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+376*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+376*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall376 ENDP

WrapperCall377 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+377*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+377*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+377*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall377 ENDP

WrapperCall378 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+378*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+378*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+378*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall378 ENDP

WrapperCall379 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+379*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+379*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+379*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall379 ENDP

WrapperCall380 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+380*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+380*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+380*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall380 ENDP

WrapperCall381 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+381*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+381*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+381*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall381 ENDP

WrapperCall382 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+382*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+382*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+382*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall382 ENDP

WrapperCall383 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+383*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+383*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+383*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall383 ENDP

WrapperCall384 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+384*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+384*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+384*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall384 ENDP

WrapperCall385 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+385*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+385*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+385*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall385 ENDP

WrapperCall386 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+386*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+386*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+386*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall386 ENDP

WrapperCall387 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+387*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+387*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+387*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall387 ENDP

WrapperCall388 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+388*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+388*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+388*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall388 ENDP

WrapperCall389 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+389*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+389*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+389*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall389 ENDP

WrapperCall390 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+390*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+390*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+390*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall390 ENDP

WrapperCall391 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+391*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+391*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+391*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall391 ENDP

WrapperCall392 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+392*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+392*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+392*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall392 ENDP

WrapperCall393 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+393*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+393*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+393*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall393 ENDP

WrapperCall394 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+394*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+394*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+394*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall394 ENDP

WrapperCall395 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+395*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+395*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+395*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall395 ENDP

WrapperCall396 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+396*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+396*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+396*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall396 ENDP

WrapperCall397 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+397*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+397*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+397*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall397 ENDP

WrapperCall398 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+398*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+398*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+398*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall398 ENDP

WrapperCall399 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+399*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+399*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+399*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall399 ENDP

WrapperCall400 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+400*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+400*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+400*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall400 ENDP

WrapperCall401 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+401*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+401*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+401*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall401 ENDP

WrapperCall402 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+402*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+402*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+402*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall402 ENDP

WrapperCall403 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+403*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+403*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+403*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall403 ENDP

WrapperCall404 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+404*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+404*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+404*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall404 ENDP

WrapperCall405 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+405*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+405*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+405*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall405 ENDP

WrapperCall406 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+406*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+406*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+406*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall406 ENDP

WrapperCall407 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+407*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+407*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+407*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall407 ENDP

WrapperCall408 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+408*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+408*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+408*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall408 ENDP

WrapperCall409 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+409*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+409*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+409*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall409 ENDP

WrapperCall410 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+410*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+410*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+410*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall410 ENDP

WrapperCall411 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+411*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+411*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+411*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall411 ENDP

WrapperCall412 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+412*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+412*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+412*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall412 ENDP

WrapperCall413 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+413*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+413*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+413*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall413 ENDP

WrapperCall414 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+414*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+414*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+414*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall414 ENDP

WrapperCall415 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+415*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+415*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+415*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall415 ENDP

WrapperCall416 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+416*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+416*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+416*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall416 ENDP

WrapperCall417 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+417*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+417*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+417*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall417 ENDP

WrapperCall418 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+418*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+418*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+418*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall418 ENDP

WrapperCall419 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+419*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+419*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+419*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall419 ENDP

WrapperCall420 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+420*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+420*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+420*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall420 ENDP

WrapperCall421 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+421*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+421*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+421*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall421 ENDP

WrapperCall422 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+422*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+422*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+422*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall422 ENDP

WrapperCall423 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+423*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+423*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+423*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall423 ENDP

WrapperCall424 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+424*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+424*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+424*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall424 ENDP

WrapperCall425 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+425*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+425*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+425*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall425 ENDP

WrapperCall426 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+426*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+426*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+426*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall426 ENDP

WrapperCall427 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+427*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+427*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+427*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall427 ENDP

WrapperCall428 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+428*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+428*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+428*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall428 ENDP

WrapperCall429 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+429*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+429*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+429*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall429 ENDP

WrapperCall430 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+430*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+430*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+430*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall430 ENDP

WrapperCall431 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+431*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+431*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+431*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall431 ENDP

WrapperCall432 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+432*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+432*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+432*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall432 ENDP

WrapperCall433 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+433*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+433*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+433*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall433 ENDP

WrapperCall434 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+434*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+434*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+434*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall434 ENDP

WrapperCall435 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+435*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+435*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+435*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall435 ENDP

WrapperCall436 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+436*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+436*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+436*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall436 ENDP

WrapperCall437 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+437*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+437*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+437*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall437 ENDP

WrapperCall438 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+438*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+438*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+438*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall438 ENDP

WrapperCall439 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+439*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+439*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+439*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall439 ENDP

WrapperCall440 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+440*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+440*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+440*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall440 ENDP

WrapperCall441 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+441*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+441*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+441*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall441 ENDP

WrapperCall442 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+442*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+442*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+442*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall442 ENDP

WrapperCall443 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+443*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+443*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+443*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall443 ENDP

WrapperCall444 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+444*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+444*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+444*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall444 ENDP

WrapperCall445 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+445*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+445*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+445*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall445 ENDP

WrapperCall446 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+446*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+446*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+446*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall446 ENDP

WrapperCall447 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+447*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+447*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+447*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall447 ENDP

WrapperCall448 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+448*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+448*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+448*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall448 ENDP

WrapperCall449 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+449*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+449*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+449*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall449 ENDP

WrapperCall450 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+450*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+450*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+450*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall450 ENDP

WrapperCall451 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+451*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+451*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+451*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall451 ENDP

WrapperCall452 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+452*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+452*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+452*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall452 ENDP

WrapperCall453 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+453*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+453*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+453*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall453 ENDP

WrapperCall454 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+454*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+454*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+454*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall454 ENDP

WrapperCall455 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+455*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+455*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+455*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall455 ENDP

WrapperCall456 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+456*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+456*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+456*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall456 ENDP

WrapperCall457 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+457*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+457*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+457*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall457 ENDP

WrapperCall458 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+458*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+458*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+458*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall458 ENDP

WrapperCall459 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+459*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+459*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+459*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall459 ENDP

WrapperCall460 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+460*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+460*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+460*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall460 ENDP

WrapperCall461 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+461*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+461*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+461*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall461 ENDP

WrapperCall462 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+462*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+462*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+462*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall462 ENDP

WrapperCall463 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+463*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+463*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+463*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall463 ENDP

WrapperCall464 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+464*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+464*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+464*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall464 ENDP

WrapperCall465 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+465*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+465*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+465*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall465 ENDP

WrapperCall466 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+466*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+466*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+466*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall466 ENDP

WrapperCall467 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+467*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+467*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+467*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall467 ENDP

WrapperCall468 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+468*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+468*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+468*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall468 ENDP

WrapperCall469 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+469*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+469*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+469*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall469 ENDP

WrapperCall470 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+470*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+470*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+470*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall470 ENDP

WrapperCall471 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+471*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+471*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+471*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall471 ENDP

WrapperCall472 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+472*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+472*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+472*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall472 ENDP

WrapperCall473 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+473*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+473*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+473*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall473 ENDP

WrapperCall474 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+474*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+474*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+474*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall474 ENDP

WrapperCall475 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+475*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+475*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+475*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall475 ENDP

WrapperCall476 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+476*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+476*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+476*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall476 ENDP

WrapperCall477 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+477*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+477*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+477*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall477 ENDP

WrapperCall478 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+478*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+478*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+478*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall478 ENDP

WrapperCall479 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+479*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+479*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+479*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall479 ENDP

WrapperCall480 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+480*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+480*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+480*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall480 ENDP

WrapperCall481 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+481*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+481*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+481*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall481 ENDP

WrapperCall482 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+482*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+482*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+482*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall482 ENDP

WrapperCall483 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+483*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+483*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+483*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall483 ENDP

WrapperCall484 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+484*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+484*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+484*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall484 ENDP

WrapperCall485 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+485*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+485*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+485*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall485 ENDP

WrapperCall486 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+486*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+486*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+486*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall486 ENDP

WrapperCall487 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+487*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+487*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+487*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall487 ENDP

WrapperCall488 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+488*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+488*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+488*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall488 ENDP

WrapperCall489 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+489*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+489*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+489*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall489 ENDP

WrapperCall490 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+490*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+490*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+490*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall490 ENDP

WrapperCall491 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+491*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+491*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+491*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall491 ENDP

WrapperCall492 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+492*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+492*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+492*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall492 ENDP

WrapperCall493 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+493*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+493*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+493*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall493 ENDP

WrapperCall494 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+494*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+494*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+494*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall494 ENDP

WrapperCall495 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+495*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+495*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+495*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall495 ENDP

WrapperCall496 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+496*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+496*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+496*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall496 ENDP

WrapperCall497 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+497*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+497*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+497*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall497 ENDP

WrapperCall498 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+498*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+498*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+498*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall498 ENDP

WrapperCall499 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+499*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+499*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+499*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall499 ENDP

WrapperCall500 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+500*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+500*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+500*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall500 ENDP

WrapperCall501 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+501*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+501*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+501*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall501 ENDP

WrapperCall502 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+502*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+502*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+502*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall502 ENDP

WrapperCall503 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+503*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+503*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+503*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall503 ENDP

WrapperCall504 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+504*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+504*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+504*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall504 ENDP

WrapperCall505 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+505*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+505*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+505*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall505 ENDP

WrapperCall506 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+506*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+506*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+506*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall506 ENDP

WrapperCall507 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+507*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+507*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+507*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall507 ENDP

WrapperCall508 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+508*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+508*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+508*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall508 ENDP

WrapperCall509 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+509*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+509*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+509*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall509 ENDP

WrapperCall510 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+510*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+510*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+510*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall510 ENDP

WrapperCall511 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+511*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+511*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+511*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall511 ENDP

WrapperCall512 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+512*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+512*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+512*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall512 ENDP

WrapperCall513 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+513*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+513*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+513*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall513 ENDP

WrapperCall514 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+514*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+514*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+514*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall514 ENDP

WrapperCall515 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+515*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+515*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+515*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall515 ENDP

WrapperCall516 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+516*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+516*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+516*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall516 ENDP

WrapperCall517 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+517*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+517*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+517*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall517 ENDP

WrapperCall518 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+518*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+518*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+518*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall518 ENDP

WrapperCall519 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+519*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+519*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+519*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall519 ENDP

WrapperCall520 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+520*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+520*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+520*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall520 ENDP

WrapperCall521 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+521*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+521*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+521*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall521 ENDP

WrapperCall522 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+522*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+522*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+522*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall522 ENDP

WrapperCall523 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+523*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+523*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+523*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall523 ENDP

WrapperCall524 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+524*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+524*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+524*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall524 ENDP

WrapperCall525 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+525*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+525*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+525*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall525 ENDP

WrapperCall526 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+526*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+526*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+526*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall526 ENDP

WrapperCall527 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+527*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+527*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+527*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall527 ENDP

WrapperCall528 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+528*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+528*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+528*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall528 ENDP

WrapperCall529 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+529*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+529*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+529*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall529 ENDP

WrapperCall530 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+530*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+530*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+530*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall530 ENDP

WrapperCall531 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+531*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+531*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+531*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall531 ENDP

WrapperCall532 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+532*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+532*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+532*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall532 ENDP

WrapperCall533 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+533*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+533*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+533*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall533 ENDP

WrapperCall534 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+534*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+534*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+534*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall534 ENDP

WrapperCall535 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+535*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+535*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+535*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall535 ENDP

WrapperCall536 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+536*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+536*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+536*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall536 ENDP

WrapperCall537 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+537*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+537*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+537*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall537 ENDP

WrapperCall538 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+538*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+538*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+538*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall538 ENDP

WrapperCall539 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+539*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+539*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+539*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall539 ENDP

WrapperCall540 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+540*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+540*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+540*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall540 ENDP

WrapperCall541 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+541*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+541*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+541*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall541 ENDP

WrapperCall542 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+542*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+542*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+542*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall542 ENDP

WrapperCall543 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+543*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+543*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+543*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall543 ENDP

WrapperCall544 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+544*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+544*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+544*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall544 ENDP

WrapperCall545 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+545*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+545*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+545*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall545 ENDP

WrapperCall546 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+546*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+546*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+546*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall546 ENDP

WrapperCall547 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+547*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+547*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+547*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall547 ENDP

WrapperCall548 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+548*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+548*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+548*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall548 ENDP

WrapperCall549 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+549*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+549*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+549*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall549 ENDP

WrapperCall550 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+550*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+550*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+550*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall550 ENDP

WrapperCall551 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+551*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+551*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+551*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall551 ENDP

WrapperCall552 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+552*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+552*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+552*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall552 ENDP

WrapperCall553 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+553*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+553*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+553*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall553 ENDP

WrapperCall554 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+554*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+554*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+554*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall554 ENDP

WrapperCall555 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+555*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+555*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+555*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall555 ENDP

WrapperCall556 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+556*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+556*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+556*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall556 ENDP

WrapperCall557 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+557*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+557*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+557*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall557 ENDP

WrapperCall558 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+558*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+558*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+558*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall558 ENDP

WrapperCall559 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+559*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+559*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+559*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall559 ENDP

WrapperCall560 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+560*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+560*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+560*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall560 ENDP

WrapperCall561 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+561*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+561*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+561*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall561 ENDP

WrapperCall562 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+562*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+562*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+562*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall562 ENDP

WrapperCall563 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+563*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+563*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+563*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall563 ENDP

WrapperCall564 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+564*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+564*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+564*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall564 ENDP

WrapperCall565 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+565*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+565*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+565*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall565 ENDP

WrapperCall566 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+566*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+566*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+566*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall566 ENDP

WrapperCall567 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+567*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+567*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+567*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall567 ENDP

WrapperCall568 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+568*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+568*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+568*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall568 ENDP

WrapperCall569 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+569*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+569*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+569*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall569 ENDP

WrapperCall570 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+570*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+570*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+570*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall570 ENDP

WrapperCall571 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+571*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+571*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+571*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall571 ENDP

WrapperCall572 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+572*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+572*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+572*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall572 ENDP

WrapperCall573 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+573*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+573*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+573*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall573 ENDP

WrapperCall574 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+574*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+574*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+574*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall574 ENDP

WrapperCall575 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+575*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+575*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+575*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall575 ENDP

WrapperCall576 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+576*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+576*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+576*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall576 ENDP

WrapperCall577 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+577*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+577*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+577*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall577 ENDP

WrapperCall578 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+578*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+578*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+578*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall578 ENDP

WrapperCall579 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+579*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+579*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+579*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall579 ENDP

WrapperCall580 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+580*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+580*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+580*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall580 ENDP

WrapperCall581 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+581*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+581*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+581*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall581 ENDP

WrapperCall582 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+582*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+582*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+582*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall582 ENDP

WrapperCall583 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+583*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+583*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+583*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall583 ENDP

WrapperCall584 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+584*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+584*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+584*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall584 ENDP

WrapperCall585 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+585*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+585*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+585*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall585 ENDP

WrapperCall586 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+586*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+586*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+586*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall586 ENDP

WrapperCall587 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+587*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+587*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+587*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall587 ENDP

WrapperCall588 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+588*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+588*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+588*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall588 ENDP

WrapperCall589 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+589*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+589*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+589*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall589 ENDP

WrapperCall590 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+590*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+590*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+590*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall590 ENDP

WrapperCall591 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+591*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+591*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+591*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall591 ENDP

WrapperCall592 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+592*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+592*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+592*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall592 ENDP

WrapperCall593 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+593*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+593*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+593*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall593 ENDP

WrapperCall594 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+594*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+594*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+594*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall594 ENDP

WrapperCall595 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+595*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+595*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+595*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall595 ENDP

WrapperCall596 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+596*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+596*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+596*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall596 ENDP

WrapperCall597 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+597*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+597*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+597*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall597 ENDP

WrapperCall598 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+598*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+598*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+598*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall598 ENDP

WrapperCall599 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+599*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+599*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+599*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall599 ENDP

WrapperCall600 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+600*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+600*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+600*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall600 ENDP

WrapperCall601 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+601*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+601*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+601*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall601 ENDP

WrapperCall602 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+602*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+602*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+602*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall602 ENDP

WrapperCall603 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+603*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+603*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+603*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall603 ENDP

WrapperCall604 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+604*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+604*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+604*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall604 ENDP

WrapperCall605 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+605*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+605*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+605*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall605 ENDP

WrapperCall606 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+606*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+606*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+606*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall606 ENDP

WrapperCall607 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+607*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+607*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+607*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall607 ENDP

WrapperCall608 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+608*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+608*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+608*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall608 ENDP

WrapperCall609 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+609*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+609*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+609*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall609 ENDP

WrapperCall610 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+610*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+610*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+610*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall610 ENDP

WrapperCall611 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+611*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+611*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+611*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall611 ENDP

WrapperCall612 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+612*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+612*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+612*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall612 ENDP

WrapperCall613 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+613*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+613*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+613*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall613 ENDP

WrapperCall614 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+614*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+614*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+614*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall614 ENDP

WrapperCall615 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+615*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+615*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+615*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall615 ENDP

WrapperCall616 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+616*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+616*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+616*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall616 ENDP

WrapperCall617 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+617*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+617*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+617*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall617 ENDP

WrapperCall618 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+618*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+618*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+618*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall618 ENDP

WrapperCall619 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+619*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+619*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+619*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall619 ENDP

WrapperCall620 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+620*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+620*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+620*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall620 ENDP

WrapperCall621 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+621*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+621*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+621*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall621 ENDP

WrapperCall622 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+622*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+622*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+622*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall622 ENDP

WrapperCall623 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+623*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+623*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+623*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall623 ENDP

WrapperCall624 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+624*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+624*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+624*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall624 ENDP

WrapperCall625 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+625*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+625*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+625*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall625 ENDP

WrapperCall626 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+626*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+626*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+626*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall626 ENDP

WrapperCall627 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+627*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+627*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+627*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall627 ENDP

WrapperCall628 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+628*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+628*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+628*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall628 ENDP

WrapperCall629 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+629*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+629*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+629*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall629 ENDP

WrapperCall630 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+630*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+630*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+630*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall630 ENDP

WrapperCall631 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+631*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+631*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+631*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall631 ENDP

WrapperCall632 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+632*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+632*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+632*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall632 ENDP

WrapperCall633 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+633*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+633*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+633*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall633 ENDP

WrapperCall634 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+634*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+634*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+634*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall634 ENDP

WrapperCall635 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+635*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+635*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+635*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall635 ENDP

WrapperCall636 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+636*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+636*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+636*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall636 ENDP

WrapperCall637 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+637*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+637*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+637*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall637 ENDP

WrapperCall638 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+638*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+638*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+638*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall638 ENDP

WrapperCall639 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+639*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+639*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+639*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall639 ENDP

WrapperCall640 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+640*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+640*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+640*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall640 ENDP

WrapperCall641 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+641*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+641*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+641*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall641 ENDP

WrapperCall642 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+642*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+642*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+642*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall642 ENDP

WrapperCall643 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+643*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+643*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+643*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall643 ENDP

WrapperCall644 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+644*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+644*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+644*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall644 ENDP

WrapperCall645 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+645*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+645*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+645*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall645 ENDP

WrapperCall646 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+646*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+646*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+646*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall646 ENDP

WrapperCall647 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+647*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+647*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+647*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall647 ENDP

WrapperCall648 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+648*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+648*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+648*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall648 ENDP

WrapperCall649 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+649*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+649*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+649*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall649 ENDP

WrapperCall650 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+650*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+650*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+650*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall650 ENDP

WrapperCall651 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+651*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+651*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+651*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall651 ENDP

WrapperCall652 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+652*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+652*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+652*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall652 ENDP

WrapperCall653 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+653*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+653*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+653*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall653 ENDP

WrapperCall654 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+654*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+654*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+654*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall654 ENDP

WrapperCall655 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+655*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+655*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+655*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall655 ENDP

WrapperCall656 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+656*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+656*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+656*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall656 ENDP

WrapperCall657 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+657*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+657*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+657*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall657 ENDP

WrapperCall658 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+658*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+658*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+658*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall658 ENDP

WrapperCall659 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+659*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+659*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+659*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall659 ENDP

WrapperCall660 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+660*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+660*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+660*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall660 ENDP

WrapperCall661 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+661*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+661*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+661*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall661 ENDP

WrapperCall662 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+662*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+662*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+662*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall662 ENDP

WrapperCall663 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+663*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+663*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+663*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall663 ENDP

WrapperCall664 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+664*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+664*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+664*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall664 ENDP

WrapperCall665 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+665*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+665*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+665*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall665 ENDP

WrapperCall666 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+666*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+666*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+666*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall666 ENDP

WrapperCall667 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+667*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+667*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+667*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall667 ENDP

WrapperCall668 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+668*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+668*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+668*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall668 ENDP

WrapperCall669 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+669*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+669*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+669*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall669 ENDP

WrapperCall670 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+670*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+670*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+670*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall670 ENDP

WrapperCall671 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+671*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+671*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+671*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall671 ENDP

WrapperCall672 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+672*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+672*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+672*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall672 ENDP

WrapperCall673 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+673*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+673*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+673*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall673 ENDP

WrapperCall674 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+674*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+674*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+674*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall674 ENDP

WrapperCall675 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+675*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+675*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+675*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall675 ENDP

WrapperCall676 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+676*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+676*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+676*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall676 ENDP

WrapperCall677 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+677*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+677*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+677*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall677 ENDP

WrapperCall678 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+678*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+678*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+678*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall678 ENDP

WrapperCall679 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+679*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+679*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+679*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall679 ENDP

WrapperCall680 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+680*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+680*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+680*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall680 ENDP

WrapperCall681 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+681*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+681*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+681*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall681 ENDP

WrapperCall682 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+682*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+682*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+682*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall682 ENDP

WrapperCall683 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+683*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+683*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+683*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall683 ENDP

WrapperCall684 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+684*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+684*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+684*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall684 ENDP

WrapperCall685 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+685*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+685*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+685*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall685 ENDP

WrapperCall686 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+686*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+686*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+686*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall686 ENDP

WrapperCall687 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+687*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+687*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+687*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall687 ENDP

WrapperCall688 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+688*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+688*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+688*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall688 ENDP

WrapperCall689 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+689*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+689*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+689*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall689 ENDP

WrapperCall690 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+690*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+690*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+690*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall690 ENDP

WrapperCall691 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+691*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+691*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+691*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall691 ENDP

WrapperCall692 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+692*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+692*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+692*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall692 ENDP

WrapperCall693 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+693*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+693*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+693*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall693 ENDP

WrapperCall694 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+694*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+694*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+694*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall694 ENDP

WrapperCall695 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+695*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+695*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+695*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall695 ENDP

WrapperCall696 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+696*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+696*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+696*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall696 ENDP

WrapperCall697 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+697*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+697*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+697*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall697 ENDP

WrapperCall698 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+698*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+698*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+698*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall698 ENDP

WrapperCall699 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+699*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+699*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+699*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall699 ENDP

WrapperCall700 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+700*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+700*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+700*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall700 ENDP

WrapperCall701 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+701*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+701*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+701*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall701 ENDP

WrapperCall702 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+702*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+702*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+702*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall702 ENDP

WrapperCall703 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+703*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+703*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+703*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall703 ENDP

WrapperCall704 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+704*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+704*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+704*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall704 ENDP

WrapperCall705 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+705*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+705*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+705*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall705 ENDP

WrapperCall706 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+706*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+706*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+706*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall706 ENDP

WrapperCall707 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+707*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+707*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+707*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall707 ENDP

WrapperCall708 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+708*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+708*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+708*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall708 ENDP

WrapperCall709 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+709*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+709*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+709*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall709 ENDP

WrapperCall710 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+710*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+710*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+710*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall710 ENDP

WrapperCall711 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+711*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+711*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+711*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall711 ENDP

WrapperCall712 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+712*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+712*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+712*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall712 ENDP

WrapperCall713 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+713*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+713*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+713*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall713 ENDP

WrapperCall714 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+714*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+714*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+714*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall714 ENDP

WrapperCall715 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+715*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+715*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+715*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall715 ENDP

WrapperCall716 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+716*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+716*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+716*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall716 ENDP

WrapperCall717 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+717*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+717*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+717*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall717 ENDP

WrapperCall718 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+718*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+718*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+718*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall718 ENDP

WrapperCall719 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+719*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+719*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+719*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall719 ENDP

WrapperCall720 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+720*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+720*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+720*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall720 ENDP

WrapperCall721 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+721*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+721*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+721*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall721 ENDP

WrapperCall722 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+722*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+722*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+722*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall722 ENDP

WrapperCall723 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+723*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+723*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+723*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall723 ENDP

WrapperCall724 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+724*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+724*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+724*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall724 ENDP

WrapperCall725 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+725*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+725*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+725*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall725 ENDP

WrapperCall726 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+726*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+726*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+726*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall726 ENDP

WrapperCall727 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+727*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+727*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+727*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall727 ENDP

WrapperCall728 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+728*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+728*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+728*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall728 ENDP

WrapperCall729 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+729*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+729*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+729*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall729 ENDP

WrapperCall730 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+730*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+730*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+730*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall730 ENDP

WrapperCall731 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+731*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+731*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+731*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall731 ENDP

WrapperCall732 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+732*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+732*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+732*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall732 ENDP

WrapperCall733 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+733*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+733*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+733*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall733 ENDP

WrapperCall734 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+734*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+734*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+734*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall734 ENDP

WrapperCall735 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+735*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+735*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+735*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall735 ENDP

WrapperCall736 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+736*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+736*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+736*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall736 ENDP

WrapperCall737 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+737*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+737*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+737*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall737 ENDP

WrapperCall738 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+738*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+738*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+738*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall738 ENDP

WrapperCall739 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+739*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+739*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+739*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall739 ENDP

WrapperCall740 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+740*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+740*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+740*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall740 ENDP

WrapperCall741 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+741*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+741*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+741*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall741 ENDP

WrapperCall742 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+742*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+742*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+742*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall742 ENDP

WrapperCall743 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+743*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+743*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+743*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall743 ENDP

WrapperCall744 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+744*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+744*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+744*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall744 ENDP

WrapperCall745 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+745*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+745*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+745*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall745 ENDP

WrapperCall746 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+746*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+746*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+746*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall746 ENDP

WrapperCall747 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+747*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+747*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+747*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall747 ENDP

WrapperCall748 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+748*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+748*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+748*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall748 ENDP

WrapperCall749 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+749*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+749*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+749*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall749 ENDP

WrapperCall750 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+750*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+750*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+750*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall750 ENDP

WrapperCall751 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+751*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+751*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+751*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall751 ENDP

WrapperCall752 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+752*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+752*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+752*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall752 ENDP

WrapperCall753 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+753*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+753*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+753*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall753 ENDP

WrapperCall754 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+754*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+754*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+754*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall754 ENDP

WrapperCall755 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+755*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+755*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+755*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall755 ENDP

WrapperCall756 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+756*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+756*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+756*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall756 ENDP

WrapperCall757 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+757*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+757*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+757*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall757 ENDP

WrapperCall758 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+758*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+758*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+758*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall758 ENDP

WrapperCall759 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+759*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+759*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+759*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall759 ENDP

WrapperCall760 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+760*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+760*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+760*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall760 ENDP

WrapperCall761 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+761*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+761*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+761*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall761 ENDP

WrapperCall762 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+762*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+762*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+762*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall762 ENDP

WrapperCall763 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+763*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+763*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+763*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall763 ENDP

WrapperCall764 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+764*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+764*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+764*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall764 ENDP

WrapperCall765 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+765*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+765*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+765*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall765 ENDP

WrapperCall766 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+766*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+766*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+766*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall766 ENDP

WrapperCall767 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+767*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+767*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+767*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall767 ENDP

WrapperCall768 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+768*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+768*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+768*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall768 ENDP

WrapperCall769 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+769*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+769*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+769*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall769 ENDP

WrapperCall770 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+770*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+770*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+770*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall770 ENDP

WrapperCall771 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+771*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+771*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+771*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall771 ENDP

WrapperCall772 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+772*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+772*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+772*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall772 ENDP

WrapperCall773 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+773*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+773*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+773*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall773 ENDP

WrapperCall774 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+774*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+774*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+774*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall774 ENDP

WrapperCall775 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+775*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+775*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+775*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall775 ENDP

WrapperCall776 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+776*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+776*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+776*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall776 ENDP

WrapperCall777 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+777*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+777*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+777*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall777 ENDP

WrapperCall778 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+778*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+778*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+778*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall778 ENDP

WrapperCall779 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+779*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+779*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+779*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall779 ENDP

WrapperCall780 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+780*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+780*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+780*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall780 ENDP

WrapperCall781 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+781*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+781*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+781*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall781 ENDP

WrapperCall782 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+782*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+782*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+782*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall782 ENDP

WrapperCall783 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+783*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+783*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+783*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall783 ENDP

WrapperCall784 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+784*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+784*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+784*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall784 ENDP

WrapperCall785 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+785*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+785*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+785*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall785 ENDP

WrapperCall786 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+786*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+786*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+786*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall786 ENDP

WrapperCall787 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+787*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+787*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+787*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall787 ENDP

WrapperCall788 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+788*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+788*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+788*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall788 ENDP

WrapperCall789 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+789*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+789*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+789*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall789 ENDP

WrapperCall790 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+790*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+790*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+790*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall790 ENDP

WrapperCall791 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+791*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+791*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+791*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall791 ENDP

WrapperCall792 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+792*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+792*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+792*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall792 ENDP

WrapperCall793 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+793*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+793*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+793*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall793 ENDP

WrapperCall794 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+794*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+794*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+794*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall794 ENDP

WrapperCall795 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+795*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+795*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+795*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall795 ENDP

WrapperCall796 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+796*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+796*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+796*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall796 ENDP

WrapperCall797 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+797*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+797*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+797*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall797 ENDP

WrapperCall798 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+798*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+798*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+798*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall798 ENDP

WrapperCall799 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+799*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+799*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+799*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall799 ENDP

WrapperCall800 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+800*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+800*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+800*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall800 ENDP

WrapperCall801 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+801*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+801*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+801*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall801 ENDP

WrapperCall802 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+802*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+802*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+802*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall802 ENDP

WrapperCall803 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+803*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+803*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+803*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall803 ENDP

WrapperCall804 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+804*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+804*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+804*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall804 ENDP

WrapperCall805 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+805*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+805*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+805*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall805 ENDP

WrapperCall806 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+806*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+806*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+806*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall806 ENDP

WrapperCall807 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+807*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+807*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+807*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall807 ENDP

WrapperCall808 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+808*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+808*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+808*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall808 ENDP

WrapperCall809 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+809*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+809*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+809*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall809 ENDP

WrapperCall810 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+810*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+810*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+810*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall810 ENDP

WrapperCall811 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+811*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+811*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+811*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall811 ENDP

WrapperCall812 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+812*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+812*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+812*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall812 ENDP

WrapperCall813 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+813*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+813*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+813*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall813 ENDP

WrapperCall814 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+814*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+814*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+814*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall814 ENDP

WrapperCall815 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+815*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+815*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+815*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall815 ENDP

WrapperCall816 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+816*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+816*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+816*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall816 ENDP

WrapperCall817 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+817*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+817*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+817*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall817 ENDP

WrapperCall818 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+818*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+818*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+818*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall818 ENDP

WrapperCall819 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+819*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+819*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+819*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall819 ENDP

WrapperCall820 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+820*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+820*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+820*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall820 ENDP

WrapperCall821 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+821*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+821*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+821*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall821 ENDP

WrapperCall822 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+822*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+822*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+822*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall822 ENDP

WrapperCall823 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+823*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+823*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+823*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall823 ENDP

WrapperCall824 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+824*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+824*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+824*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall824 ENDP

WrapperCall825 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+825*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+825*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+825*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall825 ENDP

WrapperCall826 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+826*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+826*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+826*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall826 ENDP

WrapperCall827 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+827*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+827*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+827*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall827 ENDP

WrapperCall828 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+828*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+828*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+828*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall828 ENDP

WrapperCall829 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+829*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+829*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+829*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall829 ENDP

WrapperCall830 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+830*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+830*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+830*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall830 ENDP

WrapperCall831 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+831*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+831*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+831*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall831 ENDP

WrapperCall832 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+832*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+832*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+832*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall832 ENDP

WrapperCall833 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+833*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+833*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+833*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall833 ENDP

WrapperCall834 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+834*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+834*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+834*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall834 ENDP

WrapperCall835 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+835*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+835*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+835*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall835 ENDP

WrapperCall836 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+836*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+836*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+836*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall836 ENDP

WrapperCall837 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+837*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+837*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+837*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall837 ENDP

WrapperCall838 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+838*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+838*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+838*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall838 ENDP

WrapperCall839 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+839*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+839*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+839*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall839 ENDP

WrapperCall840 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+840*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+840*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+840*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall840 ENDP

WrapperCall841 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+841*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+841*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+841*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall841 ENDP

WrapperCall842 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+842*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+842*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+842*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall842 ENDP

WrapperCall843 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+843*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+843*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+843*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall843 ENDP

WrapperCall844 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+844*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+844*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+844*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall844 ENDP

WrapperCall845 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+845*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+845*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+845*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall845 ENDP

WrapperCall846 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+846*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+846*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+846*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall846 ENDP

WrapperCall847 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+847*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+847*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+847*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall847 ENDP

WrapperCall848 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+848*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+848*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+848*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall848 ENDP

WrapperCall849 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+849*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+849*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+849*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall849 ENDP

WrapperCall850 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+850*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+850*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+850*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall850 ENDP

WrapperCall851 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+851*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+851*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+851*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall851 ENDP

WrapperCall852 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+852*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+852*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+852*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall852 ENDP

WrapperCall853 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+853*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+853*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+853*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall853 ENDP

WrapperCall854 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+854*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+854*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+854*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall854 ENDP

WrapperCall855 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+855*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+855*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+855*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall855 ENDP

WrapperCall856 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+856*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+856*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+856*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall856 ENDP

WrapperCall857 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+857*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+857*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+857*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall857 ENDP

WrapperCall858 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+858*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+858*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+858*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall858 ENDP

WrapperCall859 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+859*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+859*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+859*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall859 ENDP

WrapperCall860 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+860*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+860*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+860*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall860 ENDP

WrapperCall861 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+861*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+861*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+861*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall861 ENDP

WrapperCall862 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+862*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+862*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+862*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall862 ENDP

WrapperCall863 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+863*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+863*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+863*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall863 ENDP

WrapperCall864 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+864*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+864*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+864*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall864 ENDP

WrapperCall865 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+865*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+865*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+865*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall865 ENDP

WrapperCall866 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+866*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+866*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+866*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall866 ENDP

WrapperCall867 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+867*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+867*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+867*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall867 ENDP

WrapperCall868 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+868*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+868*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+868*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall868 ENDP

WrapperCall869 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+869*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+869*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+869*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall869 ENDP

WrapperCall870 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+870*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+870*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+870*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall870 ENDP

WrapperCall871 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+871*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+871*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+871*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall871 ENDP

WrapperCall872 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+872*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+872*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+872*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall872 ENDP

WrapperCall873 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+873*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+873*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+873*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall873 ENDP

WrapperCall874 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+874*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+874*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+874*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall874 ENDP

WrapperCall875 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+875*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+875*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+875*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall875 ENDP

WrapperCall876 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+876*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+876*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+876*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall876 ENDP

WrapperCall877 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+877*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+877*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+877*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall877 ENDP

WrapperCall878 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+878*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+878*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+878*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall878 ENDP

WrapperCall879 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+879*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+879*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+879*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall879 ENDP

WrapperCall880 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+880*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+880*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+880*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall880 ENDP

WrapperCall881 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+881*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+881*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+881*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall881 ENDP

WrapperCall882 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+882*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+882*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+882*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall882 ENDP

WrapperCall883 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+883*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+883*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+883*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall883 ENDP

WrapperCall884 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+884*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+884*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+884*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall884 ENDP

WrapperCall885 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+885*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+885*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+885*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall885 ENDP

WrapperCall886 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+886*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+886*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+886*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall886 ENDP

WrapperCall887 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+887*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+887*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+887*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall887 ENDP

WrapperCall888 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+888*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+888*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+888*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall888 ENDP

WrapperCall889 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+889*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+889*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+889*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall889 ENDP

WrapperCall890 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+890*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+890*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+890*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall890 ENDP

WrapperCall891 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+891*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+891*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+891*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall891 ENDP

WrapperCall892 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+892*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+892*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+892*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall892 ENDP

WrapperCall893 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+893*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+893*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+893*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall893 ENDP

WrapperCall894 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+894*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+894*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+894*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall894 ENDP

WrapperCall895 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+895*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+895*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+895*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall895 ENDP

WrapperCall896 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+896*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+896*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+896*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall896 ENDP

WrapperCall897 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+897*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+897*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+897*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall897 ENDP

WrapperCall898 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+898*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+898*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+898*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall898 ENDP

WrapperCall899 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+899*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+899*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+899*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall899 ENDP

WrapperCall900 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+900*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+900*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+900*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall900 ENDP

WrapperCall901 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+901*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+901*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+901*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall901 ENDP

WrapperCall902 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+902*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+902*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+902*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall902 ENDP

WrapperCall903 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+903*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+903*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+903*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall903 ENDP

WrapperCall904 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+904*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+904*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+904*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall904 ENDP

WrapperCall905 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+905*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+905*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+905*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall905 ENDP

WrapperCall906 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+906*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+906*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+906*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall906 ENDP

WrapperCall907 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+907*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+907*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+907*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall907 ENDP

WrapperCall908 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+908*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+908*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+908*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall908 ENDP

WrapperCall909 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+909*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+909*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+909*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall909 ENDP

WrapperCall910 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+910*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+910*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+910*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall910 ENDP

WrapperCall911 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+911*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+911*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+911*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall911 ENDP

WrapperCall912 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+912*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+912*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+912*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall912 ENDP

WrapperCall913 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+913*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+913*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+913*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall913 ENDP

WrapperCall914 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+914*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+914*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+914*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall914 ENDP

WrapperCall915 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+915*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+915*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+915*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall915 ENDP

WrapperCall916 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+916*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+916*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+916*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall916 ENDP

WrapperCall917 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+917*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+917*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+917*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall917 ENDP

WrapperCall918 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+918*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+918*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+918*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall918 ENDP

WrapperCall919 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+919*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+919*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+919*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall919 ENDP

WrapperCall920 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+920*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+920*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+920*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall920 ENDP

WrapperCall921 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+921*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+921*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+921*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall921 ENDP

WrapperCall922 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+922*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+922*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+922*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall922 ENDP

WrapperCall923 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+923*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+923*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+923*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall923 ENDP

WrapperCall924 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+924*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+924*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+924*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall924 ENDP

WrapperCall925 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+925*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+925*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+925*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall925 ENDP

WrapperCall926 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+926*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+926*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+926*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall926 ENDP

WrapperCall927 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+927*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+927*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+927*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall927 ENDP

WrapperCall928 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+928*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+928*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+928*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall928 ENDP

WrapperCall929 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+929*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+929*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+929*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall929 ENDP

WrapperCall930 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+930*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+930*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+930*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall930 ENDP

WrapperCall931 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+931*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+931*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+931*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall931 ENDP

WrapperCall932 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+932*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+932*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+932*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall932 ENDP

WrapperCall933 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+933*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+933*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+933*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall933 ENDP

WrapperCall934 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+934*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+934*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+934*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall934 ENDP

WrapperCall935 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+935*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+935*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+935*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall935 ENDP

WrapperCall936 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+936*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+936*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+936*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall936 ENDP

WrapperCall937 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+937*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+937*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+937*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall937 ENDP

WrapperCall938 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+938*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+938*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+938*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall938 ENDP

WrapperCall939 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+939*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+939*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+939*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall939 ENDP

WrapperCall940 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+940*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+940*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+940*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall940 ENDP

WrapperCall941 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+941*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+941*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+941*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall941 ENDP

WrapperCall942 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+942*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+942*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+942*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall942 ENDP

WrapperCall943 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+943*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+943*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+943*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall943 ENDP

WrapperCall944 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+944*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+944*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+944*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall944 ENDP

WrapperCall945 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+945*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+945*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+945*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall945 ENDP

WrapperCall946 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+946*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+946*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+946*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall946 ENDP

WrapperCall947 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+947*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+947*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+947*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall947 ENDP

WrapperCall948 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+948*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+948*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+948*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall948 ENDP

WrapperCall949 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+949*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+949*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+949*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall949 ENDP

WrapperCall950 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+950*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+950*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+950*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall950 ENDP

WrapperCall951 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+951*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+951*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+951*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall951 ENDP

WrapperCall952 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+952*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+952*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+952*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall952 ENDP

WrapperCall953 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+953*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+953*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+953*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall953 ENDP

WrapperCall954 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+954*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+954*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+954*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall954 ENDP

WrapperCall955 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+955*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+955*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+955*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall955 ENDP

WrapperCall956 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+956*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+956*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+956*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall956 ENDP

WrapperCall957 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+957*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+957*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+957*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall957 ENDP

WrapperCall958 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+958*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+958*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+958*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall958 ENDP

WrapperCall959 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+959*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+959*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+959*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall959 ENDP

WrapperCall960 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+960*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+960*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+960*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall960 ENDP

WrapperCall961 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+961*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+961*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+961*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall961 ENDP

WrapperCall962 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+962*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+962*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+962*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall962 ENDP

WrapperCall963 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+963*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+963*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+963*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall963 ENDP

WrapperCall964 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+964*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+964*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+964*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall964 ENDP

WrapperCall965 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+965*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+965*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+965*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall965 ENDP

WrapperCall966 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+966*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+966*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+966*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall966 ENDP

WrapperCall967 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+967*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+967*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+967*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall967 ENDP

WrapperCall968 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+968*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+968*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+968*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall968 ENDP

WrapperCall969 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+969*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+969*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+969*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall969 ENDP

WrapperCall970 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+970*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+970*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+970*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall970 ENDP

WrapperCall971 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+971*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+971*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+971*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall971 ENDP

WrapperCall972 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+972*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+972*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+972*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall972 ENDP

WrapperCall973 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+973*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+973*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+973*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall973 ENDP

WrapperCall974 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+974*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+974*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+974*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall974 ENDP

WrapperCall975 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+975*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+975*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+975*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall975 ENDP

WrapperCall976 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+976*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+976*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+976*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall976 ENDP

WrapperCall977 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+977*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+977*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+977*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall977 ENDP

WrapperCall978 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+978*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+978*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+978*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall978 ENDP

WrapperCall979 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+979*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+979*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+979*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall979 ENDP

WrapperCall980 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+980*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+980*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+980*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall980 ENDP

WrapperCall981 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+981*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+981*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+981*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall981 ENDP

WrapperCall982 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+982*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+982*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+982*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall982 ENDP

WrapperCall983 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+983*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+983*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+983*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall983 ENDP

WrapperCall984 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+984*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+984*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+984*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall984 ENDP

WrapperCall985 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+985*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+985*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+985*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall985 ENDP

WrapperCall986 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+986*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+986*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+986*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall986 ENDP

WrapperCall987 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+987*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+987*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+987*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall987 ENDP

WrapperCall988 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+988*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+988*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+988*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall988 ENDP

WrapperCall989 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+989*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+989*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+989*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall989 ENDP

WrapperCall990 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+990*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+990*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+990*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall990 ENDP

WrapperCall991 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+991*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+991*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+991*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall991 ENDP

WrapperCall992 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+992*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+992*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+992*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall992 ENDP

WrapperCall993 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+993*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+993*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+993*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall993 ENDP

WrapperCall994 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+994*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+994*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+994*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall994 ENDP

WrapperCall995 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+995*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+995*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+995*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall995 ENDP

WrapperCall996 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+996*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+996*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+996*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall996 ENDP

WrapperCall997 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+997*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+997*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+997*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall997 ENDP

WrapperCall998 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+998*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+998*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+998*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall998 ENDP

WrapperCall999 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+999*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+999*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+999*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall999 ENDP

WrapperCall1000 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1000*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1000*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1000*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1000 ENDP

WrapperCall1001 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1001*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1001*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1001*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1001 ENDP

WrapperCall1002 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1002*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1002*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1002*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1002 ENDP

WrapperCall1003 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1003*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1003*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1003*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1003 ENDP

WrapperCall1004 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1004*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1004*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1004*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1004 ENDP

WrapperCall1005 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1005*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1005*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1005*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1005 ENDP

WrapperCall1006 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1006*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1006*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1006*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1006 ENDP

WrapperCall1007 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1007*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1007*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1007*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1007 ENDP

WrapperCall1008 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1008*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1008*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1008*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1008 ENDP

WrapperCall1009 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1009*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1009*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1009*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1009 ENDP

WrapperCall1010 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1010*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1010*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1010*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1010 ENDP

WrapperCall1011 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1011*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1011*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1011*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1011 ENDP

WrapperCall1012 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1012*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1012*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1012*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1012 ENDP

WrapperCall1013 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1013*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1013*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1013*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1013 ENDP

WrapperCall1014 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1014*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1014*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1014*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1014 ENDP

WrapperCall1015 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1015*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1015*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1015*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1015 ENDP

WrapperCall1016 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1016*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1016*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1016*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1016 ENDP

WrapperCall1017 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1017*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1017*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1017*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1017 ENDP

WrapperCall1018 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1018*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1018*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1018*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1018 ENDP

WrapperCall1019 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1019*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1019*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1019*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1019 ENDP

WrapperCall1020 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1020*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1020*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1020*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1020 ENDP

WrapperCall1021 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1021*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1021*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1021*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1021 ENDP

WrapperCall1022 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1022*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1022*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1022*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1022 ENDP

WrapperCall1023 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1023*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1023*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1023*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1023 ENDP

WrapperCall1024 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1024*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1024*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1024*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1024 ENDP

WrapperCall1025 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1025*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1025*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1025*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1025 ENDP

WrapperCall1026 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1026*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1026*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1026*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1026 ENDP

WrapperCall1027 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1027*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1027*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1027*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1027 ENDP

WrapperCall1028 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1028*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1028*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1028*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1028 ENDP

WrapperCall1029 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1029*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1029*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1029*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1029 ENDP

WrapperCall1030 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1030*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1030*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1030*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1030 ENDP

WrapperCall1031 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1031*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1031*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1031*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1031 ENDP

WrapperCall1032 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1032*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1032*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1032*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1032 ENDP

WrapperCall1033 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1033*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1033*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1033*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1033 ENDP

WrapperCall1034 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1034*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1034*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1034*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1034 ENDP

WrapperCall1035 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1035*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1035*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1035*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1035 ENDP

WrapperCall1036 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1036*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1036*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1036*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1036 ENDP

WrapperCall1037 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1037*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1037*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1037*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1037 ENDP

WrapperCall1038 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1038*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1038*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1038*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1038 ENDP

WrapperCall1039 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1039*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1039*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1039*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1039 ENDP

WrapperCall1040 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1040*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1040*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1040*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1040 ENDP

WrapperCall1041 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1041*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1041*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1041*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1041 ENDP

WrapperCall1042 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1042*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1042*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1042*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1042 ENDP

WrapperCall1043 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1043*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1043*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1043*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1043 ENDP

WrapperCall1044 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1044*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1044*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1044*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1044 ENDP

WrapperCall1045 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1045*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1045*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1045*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1045 ENDP

WrapperCall1046 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1046*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1046*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1046*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1046 ENDP

WrapperCall1047 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1047*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1047*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1047*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1047 ENDP

WrapperCall1048 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1048*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1048*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1048*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1048 ENDP

WrapperCall1049 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1049*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1049*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1049*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1049 ENDP

WrapperCall1050 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1050*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1050*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1050*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1050 ENDP

WrapperCall1051 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1051*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1051*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1051*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1051 ENDP

WrapperCall1052 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1052*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1052*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1052*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1052 ENDP

WrapperCall1053 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1053*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1053*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1053*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1053 ENDP

WrapperCall1054 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1054*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1054*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1054*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1054 ENDP

WrapperCall1055 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1055*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1055*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1055*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1055 ENDP

WrapperCall1056 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1056*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1056*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1056*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1056 ENDP

WrapperCall1057 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1057*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1057*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1057*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1057 ENDP

WrapperCall1058 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1058*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1058*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1058*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1058 ENDP

WrapperCall1059 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1059*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1059*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1059*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1059 ENDP

WrapperCall1060 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1060*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1060*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1060*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1060 ENDP

WrapperCall1061 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1061*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1061*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1061*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1061 ENDP

WrapperCall1062 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1062*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1062*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1062*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1062 ENDP

WrapperCall1063 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1063*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1063*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1063*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1063 ENDP

WrapperCall1064 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1064*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1064*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1064*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1064 ENDP

WrapperCall1065 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1065*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1065*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1065*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1065 ENDP

WrapperCall1066 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1066*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1066*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1066*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1066 ENDP

WrapperCall1067 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1067*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1067*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1067*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1067 ENDP

WrapperCall1068 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1068*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1068*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1068*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1068 ENDP

WrapperCall1069 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1069*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1069*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1069*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1069 ENDP

WrapperCall1070 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1070*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1070*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1070*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1070 ENDP

WrapperCall1071 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1071*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1071*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1071*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1071 ENDP

WrapperCall1072 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1072*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1072*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1072*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1072 ENDP

WrapperCall1073 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1073*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1073*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1073*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1073 ENDP

WrapperCall1074 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1074*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1074*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1074*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1074 ENDP

WrapperCall1075 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1075*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1075*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1075*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1075 ENDP

WrapperCall1076 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1076*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1076*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1076*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1076 ENDP

WrapperCall1077 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1077*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1077*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1077*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1077 ENDP

WrapperCall1078 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1078*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1078*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1078*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1078 ENDP

WrapperCall1079 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1079*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1079*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1079*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1079 ENDP

WrapperCall1080 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1080*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1080*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1080*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1080 ENDP

WrapperCall1081 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1081*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1081*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1081*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1081 ENDP

WrapperCall1082 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1082*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1082*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1082*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1082 ENDP

WrapperCall1083 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1083*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1083*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1083*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1083 ENDP

WrapperCall1084 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1084*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1084*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1084*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1084 ENDP

WrapperCall1085 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1085*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1085*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1085*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1085 ENDP

WrapperCall1086 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1086*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1086*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1086*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1086 ENDP

WrapperCall1087 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1087*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1087*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1087*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1087 ENDP

WrapperCall1088 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1088*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1088*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1088*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1088 ENDP

WrapperCall1089 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1089*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1089*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1089*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1089 ENDP

WrapperCall1090 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1090*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1090*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1090*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1090 ENDP

WrapperCall1091 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1091*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1091*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1091*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1091 ENDP

WrapperCall1092 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1092*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1092*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1092*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1092 ENDP

WrapperCall1093 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1093*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1093*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1093*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1093 ENDP

WrapperCall1094 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1094*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1094*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1094*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1094 ENDP

WrapperCall1095 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1095*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1095*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1095*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1095 ENDP

WrapperCall1096 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1096*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1096*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1096*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1096 ENDP

WrapperCall1097 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1097*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1097*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1097*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1097 ENDP

WrapperCall1098 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1098*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1098*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1098*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1098 ENDP

WrapperCall1099 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1099*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1099*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1099*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1099 ENDP

WrapperCall1100 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1100*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1100*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1100*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1100 ENDP

WrapperCall1101 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1101*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1101*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1101*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1101 ENDP

WrapperCall1102 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1102*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1102*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1102*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1102 ENDP

WrapperCall1103 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1103*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1103*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1103*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1103 ENDP

WrapperCall1104 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1104*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1104*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1104*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1104 ENDP

WrapperCall1105 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1105*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1105*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1105*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1105 ENDP

WrapperCall1106 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1106*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1106*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1106*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1106 ENDP

WrapperCall1107 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1107*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1107*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1107*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1107 ENDP

WrapperCall1108 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1108*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1108*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1108*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1108 ENDP

WrapperCall1109 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1109*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1109*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1109*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1109 ENDP

WrapperCall1110 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1110*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1110*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1110*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1110 ENDP

WrapperCall1111 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1111*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1111*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1111*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1111 ENDP

WrapperCall1112 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1112*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1112*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1112*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1112 ENDP

WrapperCall1113 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1113*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1113*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1113*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1113 ENDP

WrapperCall1114 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1114*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1114*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1114*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1114 ENDP

WrapperCall1115 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1115*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1115*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1115*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1115 ENDP

WrapperCall1116 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1116*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1116*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1116*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1116 ENDP

WrapperCall1117 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1117*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1117*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1117*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1117 ENDP

WrapperCall1118 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1118*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1118*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1118*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1118 ENDP

WrapperCall1119 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1119*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1119*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1119*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1119 ENDP

WrapperCall1120 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1120*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1120*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1120*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1120 ENDP

WrapperCall1121 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1121*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1121*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1121*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1121 ENDP

WrapperCall1122 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1122*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1122*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1122*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1122 ENDP

WrapperCall1123 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1123*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1123*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1123*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1123 ENDP

WrapperCall1124 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1124*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1124*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1124*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1124 ENDP

WrapperCall1125 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1125*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1125*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1125*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1125 ENDP

WrapperCall1126 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1126*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1126*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1126*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1126 ENDP

WrapperCall1127 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1127*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1127*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1127*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1127 ENDP

WrapperCall1128 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1128*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1128*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1128*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1128 ENDP

WrapperCall1129 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1129*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1129*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1129*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1129 ENDP

WrapperCall1130 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1130*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1130*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1130*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1130 ENDP

WrapperCall1131 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1131*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1131*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1131*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1131 ENDP

WrapperCall1132 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1132*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1132*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1132*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1132 ENDP

WrapperCall1133 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1133*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1133*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1133*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1133 ENDP

WrapperCall1134 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1134*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1134*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1134*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1134 ENDP

WrapperCall1135 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1135*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1135*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1135*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1135 ENDP

WrapperCall1136 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1136*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1136*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1136*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1136 ENDP

WrapperCall1137 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1137*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1137*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1137*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1137 ENDP

WrapperCall1138 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1138*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1138*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1138*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1138 ENDP

WrapperCall1139 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1139*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1139*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1139*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1139 ENDP

WrapperCall1140 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1140*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1140*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1140*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1140 ENDP

WrapperCall1141 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1141*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1141*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1141*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1141 ENDP

WrapperCall1142 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1142*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1142*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1142*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1142 ENDP

WrapperCall1143 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1143*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1143*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1143*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1143 ENDP

WrapperCall1144 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1144*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1144*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1144*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1144 ENDP

WrapperCall1145 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1145*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1145*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1145*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1145 ENDP

WrapperCall1146 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1146*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1146*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1146*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1146 ENDP

WrapperCall1147 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1147*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1147*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1147*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1147 ENDP

WrapperCall1148 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1148*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1148*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1148*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1148 ENDP

WrapperCall1149 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1149*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1149*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1149*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1149 ENDP

WrapperCall1150 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1150*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1150*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1150*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1150 ENDP

WrapperCall1151 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1151*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1151*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1151*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1151 ENDP

WrapperCall1152 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1152*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1152*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1152*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1152 ENDP

WrapperCall1153 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1153*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1153*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1153*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1153 ENDP

WrapperCall1154 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1154*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1154*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1154*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1154 ENDP

WrapperCall1155 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1155*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1155*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1155*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1155 ENDP

WrapperCall1156 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1156*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1156*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1156*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1156 ENDP

WrapperCall1157 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1157*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1157*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1157*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1157 ENDP

WrapperCall1158 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1158*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1158*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1158*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1158 ENDP

WrapperCall1159 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1159*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1159*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1159*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1159 ENDP

WrapperCall1160 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1160*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1160*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1160*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1160 ENDP

WrapperCall1161 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1161*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1161*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1161*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1161 ENDP

WrapperCall1162 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1162*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1162*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1162*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1162 ENDP

WrapperCall1163 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1163*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1163*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1163*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1163 ENDP

WrapperCall1164 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1164*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1164*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1164*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1164 ENDP

WrapperCall1165 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1165*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1165*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1165*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1165 ENDP

WrapperCall1166 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1166*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1166*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1166*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1166 ENDP

WrapperCall1167 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1167*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1167*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1167*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1167 ENDP

WrapperCall1168 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1168*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1168*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1168*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1168 ENDP

WrapperCall1169 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1169*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1169*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1169*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1169 ENDP

WrapperCall1170 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1170*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1170*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1170*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1170 ENDP

WrapperCall1171 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1171*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1171*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1171*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1171 ENDP

WrapperCall1172 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1172*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1172*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1172*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1172 ENDP

WrapperCall1173 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1173*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1173*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1173*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1173 ENDP

WrapperCall1174 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1174*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1174*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1174*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1174 ENDP

WrapperCall1175 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1175*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1175*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1175*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1175 ENDP

WrapperCall1176 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1176*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1176*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1176*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1176 ENDP

WrapperCall1177 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1177*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1177*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1177*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1177 ENDP

WrapperCall1178 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1178*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1178*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1178*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1178 ENDP

WrapperCall1179 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1179*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1179*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1179*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1179 ENDP

WrapperCall1180 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1180*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1180*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1180*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1180 ENDP

WrapperCall1181 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1181*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1181*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1181*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1181 ENDP

WrapperCall1182 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1182*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1182*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1182*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1182 ENDP

WrapperCall1183 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1183*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1183*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1183*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1183 ENDP

WrapperCall1184 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1184*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1184*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1184*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1184 ENDP

WrapperCall1185 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1185*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1185*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1185*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1185 ENDP

WrapperCall1186 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1186*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1186*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1186*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1186 ENDP

WrapperCall1187 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1187*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1187*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1187*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1187 ENDP

WrapperCall1188 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1188*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1188*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1188*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1188 ENDP

WrapperCall1189 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1189*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1189*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1189*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1189 ENDP

WrapperCall1190 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1190*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1190*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1190*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1190 ENDP

WrapperCall1191 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1191*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1191*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1191*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1191 ENDP

WrapperCall1192 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1192*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1192*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1192*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1192 ENDP

WrapperCall1193 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1193*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1193*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1193*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1193 ENDP

WrapperCall1194 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1194*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1194*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1194*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1194 ENDP

WrapperCall1195 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1195*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1195*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1195*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1195 ENDP

WrapperCall1196 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1196*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1196*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1196*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1196 ENDP

WrapperCall1197 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1197*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1197*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1197*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1197 ENDP

WrapperCall1198 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1198*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1198*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1198*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1198 ENDP

WrapperCall1199 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1199*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1199*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1199*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1199 ENDP

WrapperCall1200 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1200*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1200*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1200*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1200 ENDP

WrapperCall1201 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1201*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1201*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1201*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1201 ENDP

WrapperCall1202 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1202*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1202*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1202*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1202 ENDP

WrapperCall1203 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1203*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1203*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1203*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1203 ENDP

WrapperCall1204 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1204*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1204*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1204*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1204 ENDP

WrapperCall1205 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1205*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1205*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1205*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1205 ENDP

WrapperCall1206 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1206*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1206*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1206*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1206 ENDP

WrapperCall1207 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1207*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1207*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1207*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1207 ENDP

WrapperCall1208 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1208*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1208*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1208*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1208 ENDP

WrapperCall1209 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1209*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1209*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1209*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1209 ENDP

WrapperCall1210 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1210*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1210*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1210*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1210 ENDP

WrapperCall1211 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1211*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1211*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1211*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1211 ENDP

WrapperCall1212 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1212*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1212*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1212*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1212 ENDP

WrapperCall1213 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1213*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1213*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1213*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1213 ENDP

WrapperCall1214 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1214*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1214*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1214*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1214 ENDP

WrapperCall1215 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1215*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1215*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1215*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1215 ENDP

WrapperCall1216 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1216*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1216*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1216*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1216 ENDP

WrapperCall1217 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1217*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1217*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1217*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1217 ENDP

WrapperCall1218 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1218*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1218*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1218*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1218 ENDP

WrapperCall1219 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1219*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1219*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1219*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1219 ENDP

WrapperCall1220 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1220*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1220*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1220*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1220 ENDP

WrapperCall1221 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1221*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1221*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1221*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1221 ENDP

WrapperCall1222 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1222*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1222*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1222*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1222 ENDP

WrapperCall1223 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1223*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1223*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1223*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1223 ENDP

WrapperCall1224 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1224*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1224*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1224*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1224 ENDP

WrapperCall1225 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1225*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1225*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1225*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1225 ENDP

WrapperCall1226 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1226*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1226*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1226*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1226 ENDP

WrapperCall1227 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1227*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1227*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1227*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1227 ENDP

WrapperCall1228 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1228*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1228*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1228*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1228 ENDP

WrapperCall1229 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1229*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1229*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1229*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1229 ENDP

WrapperCall1230 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1230*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1230*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1230*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1230 ENDP

WrapperCall1231 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1231*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1231*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1231*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1231 ENDP

WrapperCall1232 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1232*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1232*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1232*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1232 ENDP

WrapperCall1233 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1233*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1233*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1233*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1233 ENDP

WrapperCall1234 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1234*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1234*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1234*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1234 ENDP

WrapperCall1235 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1235*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1235*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1235*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1235 ENDP

WrapperCall1236 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1236*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1236*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1236*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1236 ENDP

WrapperCall1237 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1237*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1237*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1237*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1237 ENDP

WrapperCall1238 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1238*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1238*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1238*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1238 ENDP

WrapperCall1239 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1239*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1239*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1239*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1239 ENDP

WrapperCall1240 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1240*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1240*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1240*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1240 ENDP

WrapperCall1241 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1241*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1241*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1241*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1241 ENDP

WrapperCall1242 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1242*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1242*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1242*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1242 ENDP

WrapperCall1243 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1243*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1243*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1243*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1243 ENDP

WrapperCall1244 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1244*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1244*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1244*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1244 ENDP

WrapperCall1245 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1245*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1245*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1245*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1245 ENDP

WrapperCall1246 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1246*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1246*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1246*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1246 ENDP

WrapperCall1247 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1247*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1247*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1247*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1247 ENDP

WrapperCall1248 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1248*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1248*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1248*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1248 ENDP

WrapperCall1249 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1249*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1249*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1249*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1249 ENDP

WrapperCall1250 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1250*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1250*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1250*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1250 ENDP

WrapperCall1251 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1251*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1251*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1251*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1251 ENDP

WrapperCall1252 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1252*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1252*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1252*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1252 ENDP

WrapperCall1253 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1253*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1253*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1253*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1253 ENDP

WrapperCall1254 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1254*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1254*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1254*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1254 ENDP

WrapperCall1255 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1255*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1255*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1255*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1255 ENDP

WrapperCall1256 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1256*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1256*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1256*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1256 ENDP

WrapperCall1257 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1257*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1257*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1257*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1257 ENDP

WrapperCall1258 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1258*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1258*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1258*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1258 ENDP

WrapperCall1259 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1259*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1259*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1259*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1259 ENDP

WrapperCall1260 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1260*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1260*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1260*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1260 ENDP

WrapperCall1261 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1261*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1261*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1261*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1261 ENDP

WrapperCall1262 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1262*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1262*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1262*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1262 ENDP

WrapperCall1263 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1263*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1263*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1263*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1263 ENDP

WrapperCall1264 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1264*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1264*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1264*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1264 ENDP

WrapperCall1265 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1265*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1265*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1265*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1265 ENDP

WrapperCall1266 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1266*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1266*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1266*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1266 ENDP

WrapperCall1267 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1267*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1267*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1267*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1267 ENDP

WrapperCall1268 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1268*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1268*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1268*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1268 ENDP

WrapperCall1269 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1269*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1269*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1269*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1269 ENDP

WrapperCall1270 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1270*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1270*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1270*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1270 ENDP

WrapperCall1271 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1271*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1271*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1271*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1271 ENDP

WrapperCall1272 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1272*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1272*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1272*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1272 ENDP

WrapperCall1273 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1273*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1273*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1273*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1273 ENDP

WrapperCall1274 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1274*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1274*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1274*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1274 ENDP

WrapperCall1275 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1275*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1275*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1275*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1275 ENDP

WrapperCall1276 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1276*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1276*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1276*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1276 ENDP

WrapperCall1277 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1277*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1277*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1277*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1277 ENDP

WrapperCall1278 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1278*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1278*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1278*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1278 ENDP

WrapperCall1279 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1279*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1279*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1279*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1279 ENDP

WrapperCall1280 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1280*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1280*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1280*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1280 ENDP

WrapperCall1281 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1281*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1281*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1281*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1281 ENDP

WrapperCall1282 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1282*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1282*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1282*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1282 ENDP

WrapperCall1283 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1283*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1283*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1283*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1283 ENDP

WrapperCall1284 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1284*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1284*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1284*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1284 ENDP

WrapperCall1285 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1285*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1285*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1285*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1285 ENDP

WrapperCall1286 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1286*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1286*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1286*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1286 ENDP

WrapperCall1287 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1287*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1287*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1287*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1287 ENDP

WrapperCall1288 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1288*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1288*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1288*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1288 ENDP

WrapperCall1289 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1289*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1289*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1289*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1289 ENDP

WrapperCall1290 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1290*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1290*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1290*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1290 ENDP

WrapperCall1291 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1291*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1291*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1291*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1291 ENDP

WrapperCall1292 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1292*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1292*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1292*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1292 ENDP

WrapperCall1293 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1293*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1293*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1293*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1293 ENDP

WrapperCall1294 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1294*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1294*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1294*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1294 ENDP

WrapperCall1295 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1295*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1295*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1295*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1295 ENDP

WrapperCall1296 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1296*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1296*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1296*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1296 ENDP

WrapperCall1297 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1297*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1297*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1297*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1297 ENDP

WrapperCall1298 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1298*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1298*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1298*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1298 ENDP

WrapperCall1299 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1299*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1299*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1299*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1299 ENDP

WrapperCall1300 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1300*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1300*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1300*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1300 ENDP

WrapperCall1301 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1301*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1301*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1301*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1301 ENDP

WrapperCall1302 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1302*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1302*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1302*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1302 ENDP

WrapperCall1303 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1303*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1303*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1303*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1303 ENDP

WrapperCall1304 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1304*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1304*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1304*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1304 ENDP

WrapperCall1305 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1305*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1305*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1305*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1305 ENDP

WrapperCall1306 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1306*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1306*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1306*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1306 ENDP

WrapperCall1307 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1307*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1307*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1307*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1307 ENDP

WrapperCall1308 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1308*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1308*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1308*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1308 ENDP

WrapperCall1309 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1309*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1309*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1309*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1309 ENDP

WrapperCall1310 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1310*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1310*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1310*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1310 ENDP

WrapperCall1311 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1311*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1311*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1311*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1311 ENDP

WrapperCall1312 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1312*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1312*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1312*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1312 ENDP

WrapperCall1313 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1313*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1313*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1313*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1313 ENDP

WrapperCall1314 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1314*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1314*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1314*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1314 ENDP

WrapperCall1315 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1315*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1315*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1315*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1315 ENDP

WrapperCall1316 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1316*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1316*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1316*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1316 ENDP

WrapperCall1317 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1317*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1317*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1317*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1317 ENDP

WrapperCall1318 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1318*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1318*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1318*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1318 ENDP

WrapperCall1319 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1319*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1319*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1319*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1319 ENDP

WrapperCall1320 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1320*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1320*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1320*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1320 ENDP

WrapperCall1321 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1321*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1321*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1321*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1321 ENDP

WrapperCall1322 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1322*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1322*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1322*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1322 ENDP

WrapperCall1323 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1323*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1323*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1323*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1323 ENDP

WrapperCall1324 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1324*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1324*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1324*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1324 ENDP

WrapperCall1325 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1325*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1325*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1325*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1325 ENDP

WrapperCall1326 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1326*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1326*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1326*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1326 ENDP

WrapperCall1327 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1327*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1327*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1327*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1327 ENDP

WrapperCall1328 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1328*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1328*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1328*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1328 ENDP

WrapperCall1329 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1329*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1329*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1329*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1329 ENDP

WrapperCall1330 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1330*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1330*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1330*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1330 ENDP

WrapperCall1331 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1331*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1331*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1331*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1331 ENDP

WrapperCall1332 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1332*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1332*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1332*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1332 ENDP

WrapperCall1333 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1333*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1333*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1333*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1333 ENDP

WrapperCall1334 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1334*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1334*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1334*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1334 ENDP

WrapperCall1335 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1335*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1335*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1335*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1335 ENDP

WrapperCall1336 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1336*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1336*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1336*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1336 ENDP

WrapperCall1337 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1337*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1337*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1337*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1337 ENDP

WrapperCall1338 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1338*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1338*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1338*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1338 ENDP

WrapperCall1339 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1339*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1339*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1339*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1339 ENDP

WrapperCall1340 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1340*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1340*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1340*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1340 ENDP

WrapperCall1341 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1341*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1341*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1341*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1341 ENDP

WrapperCall1342 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1342*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1342*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1342*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1342 ENDP

WrapperCall1343 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1343*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1343*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1343*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1343 ENDP

WrapperCall1344 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1344*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1344*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1344*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1344 ENDP

WrapperCall1345 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1345*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1345*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1345*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1345 ENDP

WrapperCall1346 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1346*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1346*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1346*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1346 ENDP

WrapperCall1347 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1347*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1347*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1347*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1347 ENDP

WrapperCall1348 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1348*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1348*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1348*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1348 ENDP

WrapperCall1349 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1349*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1349*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1349*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1349 ENDP

WrapperCall1350 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1350*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1350*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1350*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1350 ENDP

WrapperCall1351 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1351*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1351*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1351*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1351 ENDP

WrapperCall1352 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1352*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1352*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1352*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1352 ENDP

WrapperCall1353 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1353*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1353*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1353*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1353 ENDP

WrapperCall1354 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1354*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1354*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1354*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1354 ENDP

WrapperCall1355 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1355*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1355*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1355*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1355 ENDP

WrapperCall1356 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1356*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1356*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1356*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1356 ENDP

WrapperCall1357 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1357*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1357*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1357*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1357 ENDP

WrapperCall1358 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1358*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1358*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1358*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1358 ENDP

WrapperCall1359 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1359*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1359*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1359*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1359 ENDP

WrapperCall1360 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1360*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1360*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1360*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1360 ENDP

WrapperCall1361 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1361*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1361*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1361*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1361 ENDP

WrapperCall1362 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1362*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1362*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1362*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1362 ENDP

WrapperCall1363 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1363*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1363*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1363*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1363 ENDP

WrapperCall1364 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1364*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1364*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1364*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1364 ENDP

WrapperCall1365 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1365*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1365*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1365*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1365 ENDP

WrapperCall1366 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1366*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1366*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1366*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1366 ENDP

WrapperCall1367 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1367*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1367*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1367*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1367 ENDP

WrapperCall1368 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1368*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1368*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1368*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1368 ENDP

WrapperCall1369 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1369*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1369*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1369*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1369 ENDP

WrapperCall1370 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1370*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1370*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1370*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1370 ENDP

WrapperCall1371 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1371*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1371*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1371*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1371 ENDP

WrapperCall1372 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1372*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1372*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1372*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1372 ENDP

WrapperCall1373 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1373*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1373*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1373*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1373 ENDP

WrapperCall1374 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1374*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1374*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1374*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1374 ENDP

WrapperCall1375 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1375*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1375*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1375*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1375 ENDP

WrapperCall1376 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1376*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1376*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1376*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1376 ENDP

WrapperCall1377 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1377*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1377*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1377*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1377 ENDP

WrapperCall1378 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1378*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1378*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1378*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1378 ENDP

WrapperCall1379 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1379*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1379*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1379*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1379 ENDP

WrapperCall1380 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1380*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1380*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1380*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1380 ENDP

WrapperCall1381 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1381*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1381*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1381*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1381 ENDP

WrapperCall1382 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1382*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1382*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1382*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1382 ENDP

WrapperCall1383 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1383*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1383*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1383*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1383 ENDP

WrapperCall1384 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1384*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1384*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1384*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1384 ENDP

WrapperCall1385 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1385*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1385*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1385*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1385 ENDP

WrapperCall1386 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1386*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1386*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1386*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1386 ENDP

WrapperCall1387 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1387*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1387*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1387*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1387 ENDP

WrapperCall1388 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1388*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1388*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1388*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1388 ENDP

WrapperCall1389 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1389*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1389*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1389*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1389 ENDP

WrapperCall1390 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1390*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1390*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1390*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1390 ENDP

WrapperCall1391 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1391*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1391*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1391*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1391 ENDP

WrapperCall1392 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1392*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1392*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1392*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1392 ENDP

WrapperCall1393 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1393*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1393*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1393*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1393 ENDP

WrapperCall1394 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1394*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1394*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1394*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1394 ENDP

WrapperCall1395 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1395*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1395*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1395*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1395 ENDP

WrapperCall1396 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1396*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1396*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1396*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1396 ENDP

WrapperCall1397 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1397*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1397*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1397*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1397 ENDP

WrapperCall1398 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1398*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1398*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1398*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1398 ENDP

WrapperCall1399 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1399*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1399*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1399*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1399 ENDP

WrapperCall1400 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1400*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1400*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1400*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1400 ENDP

WrapperCall1401 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1401*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1401*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1401*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1401 ENDP

WrapperCall1402 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1402*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1402*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1402*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1402 ENDP

WrapperCall1403 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1403*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1403*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1403*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1403 ENDP

WrapperCall1404 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1404*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1404*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1404*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1404 ENDP

WrapperCall1405 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1405*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1405*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1405*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1405 ENDP

WrapperCall1406 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1406*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1406*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1406*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1406 ENDP

WrapperCall1407 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1407*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1407*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1407*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1407 ENDP

WrapperCall1408 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1408*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1408*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1408*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1408 ENDP

WrapperCall1409 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1409*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1409*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1409*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1409 ENDP

WrapperCall1410 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1410*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1410*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1410*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1410 ENDP

WrapperCall1411 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1411*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1411*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1411*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1411 ENDP

WrapperCall1412 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1412*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1412*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1412*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1412 ENDP

WrapperCall1413 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1413*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1413*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1413*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1413 ENDP

WrapperCall1414 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1414*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1414*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1414*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1414 ENDP

WrapperCall1415 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1415*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1415*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1415*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1415 ENDP

WrapperCall1416 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1416*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1416*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1416*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1416 ENDP

WrapperCall1417 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1417*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1417*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1417*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1417 ENDP

WrapperCall1418 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1418*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1418*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1418*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1418 ENDP

WrapperCall1419 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1419*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1419*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1419*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1419 ENDP

WrapperCall1420 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1420*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1420*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1420*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1420 ENDP

WrapperCall1421 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1421*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1421*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1421*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1421 ENDP

WrapperCall1422 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1422*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1422*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1422*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1422 ENDP

WrapperCall1423 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1423*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1423*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1423*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1423 ENDP

WrapperCall1424 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1424*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1424*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1424*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1424 ENDP

WrapperCall1425 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1425*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1425*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1425*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1425 ENDP

WrapperCall1426 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1426*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1426*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1426*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1426 ENDP

WrapperCall1427 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1427*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1427*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1427*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1427 ENDP

WrapperCall1428 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1428*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1428*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1428*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1428 ENDP

WrapperCall1429 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1429*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1429*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1429*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1429 ENDP

WrapperCall1430 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1430*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1430*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1430*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1430 ENDP

WrapperCall1431 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1431*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1431*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1431*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1431 ENDP

WrapperCall1432 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1432*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1432*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1432*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1432 ENDP

WrapperCall1433 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1433*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1433*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1433*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1433 ENDP

WrapperCall1434 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1434*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1434*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1434*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1434 ENDP

WrapperCall1435 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1435*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1435*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1435*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1435 ENDP

WrapperCall1436 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1436*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1436*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1436*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1436 ENDP

WrapperCall1437 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1437*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1437*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1437*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1437 ENDP

WrapperCall1438 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1438*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1438*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1438*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1438 ENDP

WrapperCall1439 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1439*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1439*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1439*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1439 ENDP

WrapperCall1440 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1440*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1440*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1440*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1440 ENDP

WrapperCall1441 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1441*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1441*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1441*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1441 ENDP

WrapperCall1442 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1442*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1442*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1442*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1442 ENDP

WrapperCall1443 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1443*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1443*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1443*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1443 ENDP

WrapperCall1444 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1444*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1444*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1444*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1444 ENDP

WrapperCall1445 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1445*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1445*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1445*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1445 ENDP

WrapperCall1446 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1446*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1446*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1446*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1446 ENDP

WrapperCall1447 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1447*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1447*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1447*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1447 ENDP

WrapperCall1448 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1448*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1448*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1448*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1448 ENDP

WrapperCall1449 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1449*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1449*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1449*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1449 ENDP

WrapperCall1450 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1450*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1450*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1450*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1450 ENDP

WrapperCall1451 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1451*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1451*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1451*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1451 ENDP

WrapperCall1452 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1452*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1452*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1452*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1452 ENDP

WrapperCall1453 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1453*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1453*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1453*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1453 ENDP

WrapperCall1454 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1454*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1454*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1454*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1454 ENDP

WrapperCall1455 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1455*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1455*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1455*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1455 ENDP

WrapperCall1456 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1456*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1456*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1456*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1456 ENDP

WrapperCall1457 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1457*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1457*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1457*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1457 ENDP

WrapperCall1458 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1458*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1458*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1458*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1458 ENDP

WrapperCall1459 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1459*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1459*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1459*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1459 ENDP

WrapperCall1460 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1460*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1460*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1460*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1460 ENDP

WrapperCall1461 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1461*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1461*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1461*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1461 ENDP

WrapperCall1462 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1462*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1462*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1462*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1462 ENDP

WrapperCall1463 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1463*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1463*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1463*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1463 ENDP

WrapperCall1464 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1464*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1464*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1464*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1464 ENDP

WrapperCall1465 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1465*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1465*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1465*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1465 ENDP

WrapperCall1466 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1466*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1466*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1466*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1466 ENDP

WrapperCall1467 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1467*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1467*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1467*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1467 ENDP

WrapperCall1468 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1468*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1468*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1468*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1468 ENDP

WrapperCall1469 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1469*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1469*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1469*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1469 ENDP

WrapperCall1470 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1470*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1470*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1470*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1470 ENDP

WrapperCall1471 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1471*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1471*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1471*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1471 ENDP

WrapperCall1472 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1472*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1472*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1472*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1472 ENDP

WrapperCall1473 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1473*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1473*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1473*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1473 ENDP

WrapperCall1474 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1474*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1474*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1474*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1474 ENDP

WrapperCall1475 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1475*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1475*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1475*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1475 ENDP

WrapperCall1476 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1476*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1476*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1476*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1476 ENDP

WrapperCall1477 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1477*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1477*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1477*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1477 ENDP

WrapperCall1478 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1478*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1478*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1478*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1478 ENDP

WrapperCall1479 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1479*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1479*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1479*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1479 ENDP

WrapperCall1480 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1480*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1480*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1480*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1480 ENDP

WrapperCall1481 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1481*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1481*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1481*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1481 ENDP

WrapperCall1482 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1482*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1482*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1482*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1482 ENDP

WrapperCall1483 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1483*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1483*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1483*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1483 ENDP

WrapperCall1484 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1484*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1484*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1484*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1484 ENDP

WrapperCall1485 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1485*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1485*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1485*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1485 ENDP

WrapperCall1486 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1486*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1486*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1486*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1486 ENDP

WrapperCall1487 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1487*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1487*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1487*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1487 ENDP

WrapperCall1488 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1488*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1488*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1488*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1488 ENDP

WrapperCall1489 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1489*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1489*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1489*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1489 ENDP

WrapperCall1490 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1490*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1490*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1490*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1490 ENDP

WrapperCall1491 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1491*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1491*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1491*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1491 ENDP

WrapperCall1492 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1492*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1492*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1492*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1492 ENDP

WrapperCall1493 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1493*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1493*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1493*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1493 ENDP

WrapperCall1494 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1494*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1494*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1494*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1494 ENDP

WrapperCall1495 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1495*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1495*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1495*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1495 ENDP

WrapperCall1496 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1496*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1496*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1496*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1496 ENDP

WrapperCall1497 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1497*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1497*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1497*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1497 ENDP

WrapperCall1498 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1498*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1498*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1498*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1498 ENDP

WrapperCall1499 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1499*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1499*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1499*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1499 ENDP

WrapperCall1500 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1500*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1500*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1500*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1500 ENDP

WrapperCall1501 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1501*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1501*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1501*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1501 ENDP

WrapperCall1502 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1502*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1502*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1502*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1502 ENDP

WrapperCall1503 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1503*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1503*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1503*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1503 ENDP

WrapperCall1504 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1504*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1504*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1504*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1504 ENDP

WrapperCall1505 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1505*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1505*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1505*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1505 ENDP

WrapperCall1506 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1506*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1506*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1506*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1506 ENDP

WrapperCall1507 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1507*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1507*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1507*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1507 ENDP

WrapperCall1508 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1508*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1508*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1508*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1508 ENDP

WrapperCall1509 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1509*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1509*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1509*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1509 ENDP

WrapperCall1510 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1510*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1510*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1510*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1510 ENDP

WrapperCall1511 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1511*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1511*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1511*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1511 ENDP

WrapperCall1512 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1512*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1512*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1512*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1512 ENDP

WrapperCall1513 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1513*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1513*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1513*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1513 ENDP

WrapperCall1514 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1514*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1514*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1514*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1514 ENDP

WrapperCall1515 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1515*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1515*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1515*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1515 ENDP

WrapperCall1516 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1516*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1516*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1516*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1516 ENDP

WrapperCall1517 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1517*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1517*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1517*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1517 ENDP

WrapperCall1518 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1518*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1518*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1518*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1518 ENDP

WrapperCall1519 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1519*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1519*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1519*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1519 ENDP

WrapperCall1520 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1520*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1520*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1520*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1520 ENDP

WrapperCall1521 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1521*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1521*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1521*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1521 ENDP

WrapperCall1522 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1522*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1522*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1522*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1522 ENDP

WrapperCall1523 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1523*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1523*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1523*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1523 ENDP

WrapperCall1524 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1524*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1524*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1524*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1524 ENDP

WrapperCall1525 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1525*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1525*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1525*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1525 ENDP

WrapperCall1526 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1526*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1526*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1526*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1526 ENDP

WrapperCall1527 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1527*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1527*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1527*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1527 ENDP

WrapperCall1528 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1528*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1528*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1528*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1528 ENDP

WrapperCall1529 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1529*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1529*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1529*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1529 ENDP

WrapperCall1530 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1530*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1530*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1530*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1530 ENDP

WrapperCall1531 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1531*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1531*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1531*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1531 ENDP

WrapperCall1532 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1532*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1532*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1532*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1532 ENDP

WrapperCall1533 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1533*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1533*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1533*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1533 ENDP

WrapperCall1534 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1534*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1534*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1534*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1534 ENDP

WrapperCall1535 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1535*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1535*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1535*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1535 ENDP

WrapperCall1536 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1536*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1536*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1536*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1536 ENDP

WrapperCall1537 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1537*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1537*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1537*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1537 ENDP

WrapperCall1538 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1538*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1538*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1538*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1538 ENDP

WrapperCall1539 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1539*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1539*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1539*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1539 ENDP

WrapperCall1540 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1540*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1540*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1540*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1540 ENDP

WrapperCall1541 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1541*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1541*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1541*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1541 ENDP

WrapperCall1542 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1542*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1542*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1542*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1542 ENDP

WrapperCall1543 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1543*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1543*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1543*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1543 ENDP

WrapperCall1544 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1544*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1544*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1544*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1544 ENDP

WrapperCall1545 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1545*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1545*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1545*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1545 ENDP

WrapperCall1546 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1546*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1546*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1546*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1546 ENDP

WrapperCall1547 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1547*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1547*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1547*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1547 ENDP

WrapperCall1548 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1548*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1548*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1548*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1548 ENDP

WrapperCall1549 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1549*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1549*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1549*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1549 ENDP

WrapperCall1550 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1550*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1550*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1550*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1550 ENDP

WrapperCall1551 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1551*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1551*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1551*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1551 ENDP

WrapperCall1552 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1552*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1552*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1552*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1552 ENDP

WrapperCall1553 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1553*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1553*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1553*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1553 ENDP

WrapperCall1554 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1554*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1554*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1554*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1554 ENDP

WrapperCall1555 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1555*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1555*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1555*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1555 ENDP

WrapperCall1556 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1556*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1556*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1556*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1556 ENDP

WrapperCall1557 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1557*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1557*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1557*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1557 ENDP

WrapperCall1558 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1558*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1558*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1558*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1558 ENDP

WrapperCall1559 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1559*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1559*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1559*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1559 ENDP

WrapperCall1560 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1560*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1560*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1560*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1560 ENDP

WrapperCall1561 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1561*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1561*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1561*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1561 ENDP

WrapperCall1562 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1562*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1562*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1562*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1562 ENDP

WrapperCall1563 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1563*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1563*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1563*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1563 ENDP

WrapperCall1564 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1564*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1564*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1564*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1564 ENDP

WrapperCall1565 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1565*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1565*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1565*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1565 ENDP

WrapperCall1566 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1566*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1566*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1566*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1566 ENDP

WrapperCall1567 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1567*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1567*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1567*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1567 ENDP

WrapperCall1568 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1568*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1568*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1568*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1568 ENDP

WrapperCall1569 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1569*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1569*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1569*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1569 ENDP

WrapperCall1570 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1570*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1570*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1570*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1570 ENDP

WrapperCall1571 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1571*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1571*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1571*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1571 ENDP

WrapperCall1572 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1572*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1572*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1572*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1572 ENDP

WrapperCall1573 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1573*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1573*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1573*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1573 ENDP

WrapperCall1574 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1574*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1574*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1574*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1574 ENDP

WrapperCall1575 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1575*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1575*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1575*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1575 ENDP

WrapperCall1576 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1576*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1576*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1576*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1576 ENDP

WrapperCall1577 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1577*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1577*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1577*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1577 ENDP

WrapperCall1578 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1578*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1578*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1578*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1578 ENDP

WrapperCall1579 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1579*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1579*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1579*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1579 ENDP

WrapperCall1580 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1580*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1580*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1580*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1580 ENDP

WrapperCall1581 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1581*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1581*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1581*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1581 ENDP

WrapperCall1582 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1582*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1582*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1582*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1582 ENDP

WrapperCall1583 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1583*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1583*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1583*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1583 ENDP

WrapperCall1584 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1584*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1584*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1584*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1584 ENDP

WrapperCall1585 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1585*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1585*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1585*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1585 ENDP

WrapperCall1586 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1586*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1586*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1586*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1586 ENDP

WrapperCall1587 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1587*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1587*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1587*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1587 ENDP

WrapperCall1588 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1588*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1588*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1588*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1588 ENDP

WrapperCall1589 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1589*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1589*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1589*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1589 ENDP

WrapperCall1590 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1590*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1590*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1590*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1590 ENDP

WrapperCall1591 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1591*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1591*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1591*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1591 ENDP

WrapperCall1592 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1592*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1592*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1592*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1592 ENDP

WrapperCall1593 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1593*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1593*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1593*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1593 ENDP

WrapperCall1594 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1594*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1594*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1594*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1594 ENDP

WrapperCall1595 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1595*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1595*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1595*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1595 ENDP

WrapperCall1596 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1596*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1596*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1596*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1596 ENDP

WrapperCall1597 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1597*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1597*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1597*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1597 ENDP

WrapperCall1598 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1598*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1598*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1598*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1598 ENDP

WrapperCall1599 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1599*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1599*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1599*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1599 ENDP

WrapperCall1600 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1600*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1600*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1600*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1600 ENDP

WrapperCall1601 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1601*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1601*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1601*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1601 ENDP

WrapperCall1602 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1602*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1602*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1602*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1602 ENDP

WrapperCall1603 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1603*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1603*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1603*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1603 ENDP

WrapperCall1604 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1604*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1604*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1604*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1604 ENDP

WrapperCall1605 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1605*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1605*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1605*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1605 ENDP

WrapperCall1606 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1606*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1606*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1606*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1606 ENDP

WrapperCall1607 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1607*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1607*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1607*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1607 ENDP

WrapperCall1608 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1608*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1608*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1608*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1608 ENDP

WrapperCall1609 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1609*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1609*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1609*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1609 ENDP

WrapperCall1610 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1610*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1610*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1610*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1610 ENDP

WrapperCall1611 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1611*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1611*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1611*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1611 ENDP

WrapperCall1612 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1612*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1612*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1612*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1612 ENDP

WrapperCall1613 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1613*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1613*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1613*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1613 ENDP

WrapperCall1614 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1614*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1614*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1614*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1614 ENDP

WrapperCall1615 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1615*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1615*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1615*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1615 ENDP

WrapperCall1616 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1616*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1616*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1616*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1616 ENDP

WrapperCall1617 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1617*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1617*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1617*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1617 ENDP

WrapperCall1618 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1618*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1618*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1618*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1618 ENDP

WrapperCall1619 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1619*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1619*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1619*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1619 ENDP

WrapperCall1620 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1620*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1620*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1620*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1620 ENDP

WrapperCall1621 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1621*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1621*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1621*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1621 ENDP

WrapperCall1622 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1622*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1622*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1622*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1622 ENDP

WrapperCall1623 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1623*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1623*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1623*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1623 ENDP

WrapperCall1624 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1624*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1624*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1624*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1624 ENDP

WrapperCall1625 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1625*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1625*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1625*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1625 ENDP

WrapperCall1626 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1626*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1626*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1626*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1626 ENDP

WrapperCall1627 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1627*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1627*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1627*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1627 ENDP

WrapperCall1628 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1628*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1628*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1628*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1628 ENDP

WrapperCall1629 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1629*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1629*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1629*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1629 ENDP

WrapperCall1630 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1630*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1630*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1630*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1630 ENDP

WrapperCall1631 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1631*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1631*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1631*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1631 ENDP

WrapperCall1632 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1632*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1632*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1632*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1632 ENDP

WrapperCall1633 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1633*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1633*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1633*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1633 ENDP

WrapperCall1634 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1634*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1634*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1634*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1634 ENDP

WrapperCall1635 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1635*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1635*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1635*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1635 ENDP

WrapperCall1636 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1636*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1636*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1636*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1636 ENDP

WrapperCall1637 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1637*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1637*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1637*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1637 ENDP

WrapperCall1638 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1638*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1638*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1638*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1638 ENDP

WrapperCall1639 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1639*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1639*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1639*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1639 ENDP

WrapperCall1640 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1640*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1640*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1640*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1640 ENDP

WrapperCall1641 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1641*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1641*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1641*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1641 ENDP

WrapperCall1642 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1642*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1642*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1642*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1642 ENDP

WrapperCall1643 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1643*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1643*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1643*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1643 ENDP

WrapperCall1644 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1644*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1644*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1644*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1644 ENDP

WrapperCall1645 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1645*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1645*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1645*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1645 ENDP

WrapperCall1646 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1646*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1646*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1646*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1646 ENDP

WrapperCall1647 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1647*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1647*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1647*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1647 ENDP

WrapperCall1648 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1648*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1648*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1648*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1648 ENDP

WrapperCall1649 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1649*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1649*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1649*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1649 ENDP

WrapperCall1650 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1650*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1650*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1650*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1650 ENDP

WrapperCall1651 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1651*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1651*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1651*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1651 ENDP

WrapperCall1652 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1652*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1652*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1652*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1652 ENDP

WrapperCall1653 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1653*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1653*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1653*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1653 ENDP

WrapperCall1654 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1654*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1654*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1654*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1654 ENDP

WrapperCall1655 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1655*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1655*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1655*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1655 ENDP

WrapperCall1656 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1656*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1656*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1656*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1656 ENDP

WrapperCall1657 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1657*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1657*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1657*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1657 ENDP

WrapperCall1658 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1658*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1658*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1658*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1658 ENDP

WrapperCall1659 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1659*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1659*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1659*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1659 ENDP

WrapperCall1660 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1660*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1660*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1660*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1660 ENDP

WrapperCall1661 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1661*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1661*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1661*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1661 ENDP

WrapperCall1662 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1662*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1662*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1662*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1662 ENDP

WrapperCall1663 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1663*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1663*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1663*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1663 ENDP

WrapperCall1664 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1664*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1664*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1664*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1664 ENDP

WrapperCall1665 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1665*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1665*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1665*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1665 ENDP

WrapperCall1666 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1666*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1666*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1666*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1666 ENDP

WrapperCall1667 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1667*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1667*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1667*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1667 ENDP

WrapperCall1668 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1668*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1668*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1668*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1668 ENDP

WrapperCall1669 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1669*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1669*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1669*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1669 ENDP

WrapperCall1670 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1670*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1670*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1670*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1670 ENDP

WrapperCall1671 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1671*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1671*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1671*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1671 ENDP

WrapperCall1672 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1672*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1672*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1672*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1672 ENDP

WrapperCall1673 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1673*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1673*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1673*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1673 ENDP

WrapperCall1674 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1674*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1674*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1674*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1674 ENDP

WrapperCall1675 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1675*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1675*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1675*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1675 ENDP

WrapperCall1676 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1676*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1676*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1676*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1676 ENDP

WrapperCall1677 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1677*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1677*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1677*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1677 ENDP

WrapperCall1678 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1678*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1678*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1678*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1678 ENDP

WrapperCall1679 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1679*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1679*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1679*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1679 ENDP

WrapperCall1680 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1680*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1680*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1680*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1680 ENDP

WrapperCall1681 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1681*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1681*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1681*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1681 ENDP

WrapperCall1682 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1682*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1682*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1682*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1682 ENDP

WrapperCall1683 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1683*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1683*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1683*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1683 ENDP

WrapperCall1684 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1684*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1684*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1684*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1684 ENDP

WrapperCall1685 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1685*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1685*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1685*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1685 ENDP

WrapperCall1686 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1686*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1686*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1686*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1686 ENDP

WrapperCall1687 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1687*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1687*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1687*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1687 ENDP

WrapperCall1688 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1688*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1688*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1688*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1688 ENDP

WrapperCall1689 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1689*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1689*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1689*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1689 ENDP

WrapperCall1690 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1690*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1690*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1690*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1690 ENDP

WrapperCall1691 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1691*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1691*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1691*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1691 ENDP

WrapperCall1692 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1692*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1692*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1692*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1692 ENDP

WrapperCall1693 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1693*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1693*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1693*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1693 ENDP

WrapperCall1694 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1694*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1694*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1694*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1694 ENDP

WrapperCall1695 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1695*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1695*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1695*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1695 ENDP

WrapperCall1696 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1696*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1696*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1696*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1696 ENDP

WrapperCall1697 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1697*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1697*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1697*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1697 ENDP

WrapperCall1698 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1698*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1698*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1698*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1698 ENDP

WrapperCall1699 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1699*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1699*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1699*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1699 ENDP

WrapperCall1700 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1700*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1700*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1700*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1700 ENDP

WrapperCall1701 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1701*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1701*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1701*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1701 ENDP

WrapperCall1702 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1702*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1702*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1702*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1702 ENDP

WrapperCall1703 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1703*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1703*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1703*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1703 ENDP

WrapperCall1704 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1704*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1704*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1704*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1704 ENDP

WrapperCall1705 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1705*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1705*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1705*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1705 ENDP

WrapperCall1706 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1706*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1706*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1706*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1706 ENDP

WrapperCall1707 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1707*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1707*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1707*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1707 ENDP

WrapperCall1708 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1708*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1708*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1708*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1708 ENDP

WrapperCall1709 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1709*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1709*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1709*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1709 ENDP

WrapperCall1710 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1710*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1710*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1710*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1710 ENDP

WrapperCall1711 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1711*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1711*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1711*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1711 ENDP

WrapperCall1712 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1712*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1712*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1712*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1712 ENDP

WrapperCall1713 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1713*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1713*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1713*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1713 ENDP

WrapperCall1714 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1714*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1714*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1714*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1714 ENDP

WrapperCall1715 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1715*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1715*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1715*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1715 ENDP

WrapperCall1716 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1716*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1716*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1716*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1716 ENDP

WrapperCall1717 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1717*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1717*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1717*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1717 ENDP

WrapperCall1718 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1718*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1718*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1718*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1718 ENDP

WrapperCall1719 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1719*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1719*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1719*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1719 ENDP

WrapperCall1720 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1720*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1720*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1720*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1720 ENDP

WrapperCall1721 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1721*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1721*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1721*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1721 ENDP

WrapperCall1722 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1722*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1722*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1722*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1722 ENDP

WrapperCall1723 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1723*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1723*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1723*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1723 ENDP

WrapperCall1724 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1724*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1724*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1724*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1724 ENDP

WrapperCall1725 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1725*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1725*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1725*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1725 ENDP

WrapperCall1726 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1726*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1726*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1726*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1726 ENDP

WrapperCall1727 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1727*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1727*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1727*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1727 ENDP

WrapperCall1728 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1728*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1728*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1728*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1728 ENDP

WrapperCall1729 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1729*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1729*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1729*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1729 ENDP

WrapperCall1730 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1730*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1730*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1730*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1730 ENDP

WrapperCall1731 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1731*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1731*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1731*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1731 ENDP

WrapperCall1732 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1732*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1732*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1732*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1732 ENDP

WrapperCall1733 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1733*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1733*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1733*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1733 ENDP

WrapperCall1734 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1734*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1734*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1734*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1734 ENDP

WrapperCall1735 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1735*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1735*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1735*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1735 ENDP

WrapperCall1736 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1736*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1736*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1736*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1736 ENDP

WrapperCall1737 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1737*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1737*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1737*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1737 ENDP

WrapperCall1738 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1738*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1738*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1738*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1738 ENDP

WrapperCall1739 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1739*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1739*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1739*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1739 ENDP

WrapperCall1740 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1740*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1740*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1740*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1740 ENDP

WrapperCall1741 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1741*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1741*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1741*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1741 ENDP

WrapperCall1742 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1742*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1742*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1742*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1742 ENDP

WrapperCall1743 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1743*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1743*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1743*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1743 ENDP

WrapperCall1744 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1744*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1744*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1744*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1744 ENDP

WrapperCall1745 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1745*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1745*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1745*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1745 ENDP

WrapperCall1746 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1746*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1746*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1746*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1746 ENDP

WrapperCall1747 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1747*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1747*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1747*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1747 ENDP

WrapperCall1748 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1748*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1748*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1748*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1748 ENDP

WrapperCall1749 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1749*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1749*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1749*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1749 ENDP

WrapperCall1750 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1750*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1750*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1750*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1750 ENDP

WrapperCall1751 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1751*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1751*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1751*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1751 ENDP

WrapperCall1752 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1752*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1752*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1752*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1752 ENDP

WrapperCall1753 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1753*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1753*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1753*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1753 ENDP

WrapperCall1754 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1754*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1754*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1754*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1754 ENDP

WrapperCall1755 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1755*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1755*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1755*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1755 ENDP

WrapperCall1756 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1756*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1756*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1756*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1756 ENDP

WrapperCall1757 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1757*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1757*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1757*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1757 ENDP

WrapperCall1758 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1758*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1758*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1758*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1758 ENDP

WrapperCall1759 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1759*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1759*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1759*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1759 ENDP

WrapperCall1760 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1760*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1760*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1760*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1760 ENDP

WrapperCall1761 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1761*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1761*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1761*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1761 ENDP

WrapperCall1762 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1762*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1762*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1762*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1762 ENDP

WrapperCall1763 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1763*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1763*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1763*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1763 ENDP

WrapperCall1764 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1764*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1764*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1764*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1764 ENDP

WrapperCall1765 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1765*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1765*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1765*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1765 ENDP

WrapperCall1766 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1766*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1766*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1766*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1766 ENDP

WrapperCall1767 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1767*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1767*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1767*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1767 ENDP

WrapperCall1768 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1768*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1768*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1768*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1768 ENDP

WrapperCall1769 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1769*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1769*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1769*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1769 ENDP

WrapperCall1770 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1770*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1770*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1770*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1770 ENDP

WrapperCall1771 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1771*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1771*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1771*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1771 ENDP

WrapperCall1772 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1772*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1772*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1772*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1772 ENDP

WrapperCall1773 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1773*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1773*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1773*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1773 ENDP

WrapperCall1774 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1774*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1774*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1774*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1774 ENDP

WrapperCall1775 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1775*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1775*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1775*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1775 ENDP

WrapperCall1776 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1776*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1776*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1776*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1776 ENDP

WrapperCall1777 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1777*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1777*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1777*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1777 ENDP

WrapperCall1778 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1778*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1778*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1778*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1778 ENDP

WrapperCall1779 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1779*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1779*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1779*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1779 ENDP

WrapperCall1780 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1780*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1780*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1780*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1780 ENDP

WrapperCall1781 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1781*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1781*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1781*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1781 ENDP

WrapperCall1782 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1782*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1782*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1782*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1782 ENDP

WrapperCall1783 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1783*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1783*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1783*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1783 ENDP

WrapperCall1784 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1784*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1784*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1784*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1784 ENDP

WrapperCall1785 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1785*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1785*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1785*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1785 ENDP

WrapperCall1786 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1786*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1786*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1786*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1786 ENDP

WrapperCall1787 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1787*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1787*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1787*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1787 ENDP

WrapperCall1788 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1788*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1788*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1788*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1788 ENDP

WrapperCall1789 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1789*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1789*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1789*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1789 ENDP

WrapperCall1790 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1790*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1790*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1790*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1790 ENDP

WrapperCall1791 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1791*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1791*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1791*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1791 ENDP

WrapperCall1792 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1792*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1792*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1792*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1792 ENDP

WrapperCall1793 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1793*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1793*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1793*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1793 ENDP

WrapperCall1794 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1794*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1794*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1794*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1794 ENDP

WrapperCall1795 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1795*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1795*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1795*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1795 ENDP

WrapperCall1796 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1796*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1796*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1796*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1796 ENDP

WrapperCall1797 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1797*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1797*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1797*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1797 ENDP

WrapperCall1798 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1798*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1798*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1798*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1798 ENDP

WrapperCall1799 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1799*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1799*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1799*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1799 ENDP

WrapperCall1800 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1800*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1800*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1800*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1800 ENDP

WrapperCall1801 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1801*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1801*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1801*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1801 ENDP

WrapperCall1802 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1802*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1802*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1802*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1802 ENDP

WrapperCall1803 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1803*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1803*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1803*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1803 ENDP

WrapperCall1804 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1804*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1804*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1804*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1804 ENDP

WrapperCall1805 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1805*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1805*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1805*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1805 ENDP

WrapperCall1806 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1806*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1806*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1806*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1806 ENDP

WrapperCall1807 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1807*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1807*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1807*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1807 ENDP

WrapperCall1808 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1808*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1808*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1808*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1808 ENDP

WrapperCall1809 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1809*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1809*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1809*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1809 ENDP

WrapperCall1810 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1810*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1810*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1810*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1810 ENDP

WrapperCall1811 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1811*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1811*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1811*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1811 ENDP

WrapperCall1812 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1812*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1812*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1812*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1812 ENDP

WrapperCall1813 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1813*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1813*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1813*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1813 ENDP

WrapperCall1814 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1814*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1814*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1814*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1814 ENDP

WrapperCall1815 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1815*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1815*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1815*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1815 ENDP

WrapperCall1816 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1816*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1816*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1816*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1816 ENDP

WrapperCall1817 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1817*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1817*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1817*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1817 ENDP

WrapperCall1818 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1818*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1818*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1818*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1818 ENDP

WrapperCall1819 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1819*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1819*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1819*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1819 ENDP

WrapperCall1820 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1820*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1820*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1820*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1820 ENDP

WrapperCall1821 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1821*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1821*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1821*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1821 ENDP

WrapperCall1822 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1822*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1822*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1822*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1822 ENDP

WrapperCall1823 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1823*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1823*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1823*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1823 ENDP

WrapperCall1824 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1824*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1824*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1824*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1824 ENDP

WrapperCall1825 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1825*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1825*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1825*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1825 ENDP

WrapperCall1826 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1826*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1826*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1826*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1826 ENDP

WrapperCall1827 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1827*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1827*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1827*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1827 ENDP

WrapperCall1828 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1828*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1828*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1828*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1828 ENDP

WrapperCall1829 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1829*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1829*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1829*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1829 ENDP

WrapperCall1830 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1830*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1830*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1830*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1830 ENDP

WrapperCall1831 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1831*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1831*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1831*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1831 ENDP

WrapperCall1832 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1832*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1832*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1832*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1832 ENDP

WrapperCall1833 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1833*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1833*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1833*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1833 ENDP

WrapperCall1834 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1834*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1834*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1834*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1834 ENDP

WrapperCall1835 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1835*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1835*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1835*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1835 ENDP

WrapperCall1836 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1836*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1836*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1836*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1836 ENDP

WrapperCall1837 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1837*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1837*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1837*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1837 ENDP

WrapperCall1838 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1838*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1838*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1838*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1838 ENDP

WrapperCall1839 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1839*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1839*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1839*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1839 ENDP

WrapperCall1840 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1840*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1840*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1840*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1840 ENDP

WrapperCall1841 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1841*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1841*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1841*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1841 ENDP

WrapperCall1842 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1842*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1842*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1842*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1842 ENDP

WrapperCall1843 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1843*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1843*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1843*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1843 ENDP

WrapperCall1844 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1844*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1844*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1844*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1844 ENDP

WrapperCall1845 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1845*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1845*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1845*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1845 ENDP

WrapperCall1846 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1846*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1846*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1846*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1846 ENDP

WrapperCall1847 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1847*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1847*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1847*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1847 ENDP

WrapperCall1848 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1848*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1848*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1848*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1848 ENDP

WrapperCall1849 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1849*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1849*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1849*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1849 ENDP

WrapperCall1850 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1850*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1850*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1850*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1850 ENDP

WrapperCall1851 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1851*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1851*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1851*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1851 ENDP

WrapperCall1852 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1852*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1852*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1852*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1852 ENDP

WrapperCall1853 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1853*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1853*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1853*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1853 ENDP

WrapperCall1854 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1854*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1854*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1854*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1854 ENDP

WrapperCall1855 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1855*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1855*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1855*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1855 ENDP

WrapperCall1856 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1856*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1856*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1856*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1856 ENDP

WrapperCall1857 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1857*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1857*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1857*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1857 ENDP

WrapperCall1858 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1858*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1858*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1858*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1858 ENDP

WrapperCall1859 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1859*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1859*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1859*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1859 ENDP

WrapperCall1860 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1860*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1860*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1860*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1860 ENDP

WrapperCall1861 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1861*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1861*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1861*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1861 ENDP

WrapperCall1862 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1862*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1862*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1862*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1862 ENDP

WrapperCall1863 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1863*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1863*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1863*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1863 ENDP

WrapperCall1864 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1864*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1864*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1864*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1864 ENDP

WrapperCall1865 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1865*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1865*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1865*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1865 ENDP

WrapperCall1866 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1866*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1866*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1866*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1866 ENDP

WrapperCall1867 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1867*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1867*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1867*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1867 ENDP

WrapperCall1868 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1868*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1868*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1868*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1868 ENDP

WrapperCall1869 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1869*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1869*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1869*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1869 ENDP

WrapperCall1870 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1870*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1870*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1870*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1870 ENDP

WrapperCall1871 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1871*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1871*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1871*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1871 ENDP

WrapperCall1872 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1872*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1872*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1872*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1872 ENDP

WrapperCall1873 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1873*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1873*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1873*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1873 ENDP

WrapperCall1874 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1874*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1874*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1874*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1874 ENDP

WrapperCall1875 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1875*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1875*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1875*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1875 ENDP

WrapperCall1876 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1876*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1876*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1876*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1876 ENDP

WrapperCall1877 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1877*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1877*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1877*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1877 ENDP

WrapperCall1878 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1878*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1878*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1878*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1878 ENDP

WrapperCall1879 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1879*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1879*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1879*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1879 ENDP

WrapperCall1880 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1880*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1880*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1880*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1880 ENDP

WrapperCall1881 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1881*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1881*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1881*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1881 ENDP

WrapperCall1882 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1882*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1882*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1882*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1882 ENDP

WrapperCall1883 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1883*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1883*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1883*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1883 ENDP

WrapperCall1884 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1884*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1884*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1884*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1884 ENDP

WrapperCall1885 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1885*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1885*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1885*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1885 ENDP

WrapperCall1886 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1886*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1886*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1886*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1886 ENDP

WrapperCall1887 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1887*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1887*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1887*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1887 ENDP

WrapperCall1888 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1888*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1888*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1888*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1888 ENDP

WrapperCall1889 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1889*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1889*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1889*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1889 ENDP

WrapperCall1890 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1890*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1890*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1890*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1890 ENDP

WrapperCall1891 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1891*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1891*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1891*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1891 ENDP

WrapperCall1892 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1892*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1892*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1892*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1892 ENDP

WrapperCall1893 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1893*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1893*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1893*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1893 ENDP

WrapperCall1894 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1894*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1894*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1894*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1894 ENDP

WrapperCall1895 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1895*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1895*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1895*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1895 ENDP

WrapperCall1896 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1896*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1896*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1896*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1896 ENDP

WrapperCall1897 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1897*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1897*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1897*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1897 ENDP

WrapperCall1898 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1898*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1898*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1898*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1898 ENDP

WrapperCall1899 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1899*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1899*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1899*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1899 ENDP

WrapperCall1900 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1900*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1900*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1900*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1900 ENDP

WrapperCall1901 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1901*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1901*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1901*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1901 ENDP

WrapperCall1902 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1902*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1902*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1902*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1902 ENDP

WrapperCall1903 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1903*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1903*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1903*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1903 ENDP

WrapperCall1904 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1904*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1904*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1904*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1904 ENDP

WrapperCall1905 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1905*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1905*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1905*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1905 ENDP

WrapperCall1906 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1906*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1906*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1906*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1906 ENDP

WrapperCall1907 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1907*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1907*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1907*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1907 ENDP

WrapperCall1908 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1908*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1908*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1908*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1908 ENDP

WrapperCall1909 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1909*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1909*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1909*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1909 ENDP

WrapperCall1910 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1910*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1910*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1910*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1910 ENDP

WrapperCall1911 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1911*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1911*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1911*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1911 ENDP

WrapperCall1912 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1912*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1912*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1912*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1912 ENDP

WrapperCall1913 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1913*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1913*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1913*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1913 ENDP

WrapperCall1914 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1914*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1914*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1914*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1914 ENDP

WrapperCall1915 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1915*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1915*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1915*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1915 ENDP

WrapperCall1916 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1916*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1916*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1916*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1916 ENDP

WrapperCall1917 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1917*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1917*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1917*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1917 ENDP

WrapperCall1918 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1918*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1918*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1918*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1918 ENDP

WrapperCall1919 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1919*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1919*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1919*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1919 ENDP

WrapperCall1920 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1920*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1920*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1920*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1920 ENDP

WrapperCall1921 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1921*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1921*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1921*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1921 ENDP

WrapperCall1922 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1922*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1922*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1922*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1922 ENDP

WrapperCall1923 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1923*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1923*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1923*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1923 ENDP

WrapperCall1924 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1924*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1924*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1924*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1924 ENDP

WrapperCall1925 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1925*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1925*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1925*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1925 ENDP

WrapperCall1926 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1926*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1926*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1926*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1926 ENDP

WrapperCall1927 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1927*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1927*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1927*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1927 ENDP

WrapperCall1928 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1928*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1928*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1928*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1928 ENDP

WrapperCall1929 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1929*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1929*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1929*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1929 ENDP

WrapperCall1930 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1930*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1930*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1930*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1930 ENDP

WrapperCall1931 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1931*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1931*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1931*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1931 ENDP

WrapperCall1932 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1932*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1932*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1932*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1932 ENDP

WrapperCall1933 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1933*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1933*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1933*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1933 ENDP

WrapperCall1934 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1934*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1934*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1934*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1934 ENDP

WrapperCall1935 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1935*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1935*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1935*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1935 ENDP

WrapperCall1936 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1936*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1936*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1936*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1936 ENDP

WrapperCall1937 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1937*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1937*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1937*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1937 ENDP

WrapperCall1938 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1938*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1938*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1938*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1938 ENDP

WrapperCall1939 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1939*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1939*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1939*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1939 ENDP

WrapperCall1940 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1940*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1940*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1940*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1940 ENDP

WrapperCall1941 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1941*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1941*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1941*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1941 ENDP

WrapperCall1942 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1942*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1942*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1942*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1942 ENDP

WrapperCall1943 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1943*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1943*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1943*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1943 ENDP

WrapperCall1944 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1944*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1944*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1944*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1944 ENDP

WrapperCall1945 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1945*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1945*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1945*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1945 ENDP

WrapperCall1946 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1946*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1946*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1946*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1946 ENDP

WrapperCall1947 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1947*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1947*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1947*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1947 ENDP

WrapperCall1948 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1948*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1948*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1948*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1948 ENDP

WrapperCall1949 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1949*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1949*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1949*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1949 ENDP

WrapperCall1950 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1950*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1950*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1950*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1950 ENDP

WrapperCall1951 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1951*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1951*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1951*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1951 ENDP

WrapperCall1952 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1952*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1952*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1952*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1952 ENDP

WrapperCall1953 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1953*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1953*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1953*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1953 ENDP

WrapperCall1954 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1954*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1954*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1954*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1954 ENDP

WrapperCall1955 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1955*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1955*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1955*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1955 ENDP

WrapperCall1956 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1956*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1956*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1956*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1956 ENDP

WrapperCall1957 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1957*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1957*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1957*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1957 ENDP

WrapperCall1958 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1958*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1958*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1958*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1958 ENDP

WrapperCall1959 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1959*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1959*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1959*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1959 ENDP

WrapperCall1960 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1960*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1960*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1960*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1960 ENDP

WrapperCall1961 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1961*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1961*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1961*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1961 ENDP

WrapperCall1962 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1962*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1962*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1962*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1962 ENDP

WrapperCall1963 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1963*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1963*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1963*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1963 ENDP

WrapperCall1964 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1964*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1964*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1964*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1964 ENDP

WrapperCall1965 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1965*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1965*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1965*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1965 ENDP

WrapperCall1966 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1966*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1966*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1966*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1966 ENDP

WrapperCall1967 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1967*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1967*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1967*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1967 ENDP

WrapperCall1968 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1968*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1968*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1968*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1968 ENDP

WrapperCall1969 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1969*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1969*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1969*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1969 ENDP

WrapperCall1970 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1970*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1970*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1970*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1970 ENDP

WrapperCall1971 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1971*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1971*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1971*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1971 ENDP

WrapperCall1972 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1972*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1972*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1972*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1972 ENDP

WrapperCall1973 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1973*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1973*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1973*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1973 ENDP

WrapperCall1974 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1974*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1974*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1974*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1974 ENDP

WrapperCall1975 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1975*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1975*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1975*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1975 ENDP

WrapperCall1976 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1976*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1976*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1976*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1976 ENDP

WrapperCall1977 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1977*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1977*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1977*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1977 ENDP

WrapperCall1978 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1978*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1978*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1978*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1978 ENDP

WrapperCall1979 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1979*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1979*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1979*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1979 ENDP

WrapperCall1980 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1980*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1980*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1980*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1980 ENDP

WrapperCall1981 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1981*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1981*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1981*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1981 ENDP

WrapperCall1982 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1982*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1982*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1982*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1982 ENDP

WrapperCall1983 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1983*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1983*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1983*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1983 ENDP

WrapperCall1984 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1984*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1984*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1984*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1984 ENDP

WrapperCall1985 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1985*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1985*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1985*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1985 ENDP

WrapperCall1986 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1986*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1986*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1986*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1986 ENDP

WrapperCall1987 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1987*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1987*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1987*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1987 ENDP

WrapperCall1988 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1988*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1988*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1988*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1988 ENDP

WrapperCall1989 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1989*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1989*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1989*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1989 ENDP

WrapperCall1990 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1990*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1990*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1990*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1990 ENDP

WrapperCall1991 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1991*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1991*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1991*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1991 ENDP

WrapperCall1992 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1992*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1992*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1992*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1992 ENDP

WrapperCall1993 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1993*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1993*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1993*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1993 ENDP

WrapperCall1994 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1994*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1994*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1994*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1994 ENDP

WrapperCall1995 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1995*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1995*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1995*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1995 ENDP

WrapperCall1996 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1996*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1996*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1996*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1996 ENDP

WrapperCall1997 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1997*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1997*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1997*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1997 ENDP

WrapperCall1998 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1998*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1998*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1998*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1998 ENDP

WrapperCall1999 PROC

mov rax, QWORD PTR [rsp]

add rsp, 40


push r9
movd r9, xmm3
push r9

push r8
movd r8, xmm2
push r8

push rdx
movd rdx, xmm1
push rdx

push rcx
movd rcx, xmm0
push rcx

mov ecx, DWORD PTR [wrapperIndex+1999*4]
mov rdx, rax
mov r8, rsp

sub rsp, 32
call WrapperLogFunctionPre
add rsp, 32

pop rcx
movd xmm0, rcx
pop rcx

pop rdx
movd xmm1, rdx
pop rdx

pop r8
movd xmm2, r8
pop r8

pop r9
movd xmm3, r9
pop r9

sub rsp, 32

mov rax, QWORD PTR [extFunctions+1999*8]
call rax

add rsp, 32

push rax

mov ecx, DWORD PTR [wrapperIndex+1999*4]
mov rdx, rax

sub rsp, 40
call WrapperLogFunctionPost
add rsp, 40

pop rcx
sub rsp, 32

push rax

mov rax, rcx

ret

WrapperCall1999 ENDP

END